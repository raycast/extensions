export interface FormValues {
  format: string;
  compression: string;
  addSuffix: boolean;
  resizeMode: string;
  resizeValue: string;
  resizeFilter: string;
  addResolutionSuffix: boolean;
  compressionLevel?: string;
}
