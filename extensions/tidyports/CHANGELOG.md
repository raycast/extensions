# TidyPorts Changelog

## [Initial Version] - {PR_MERGE_DATE}

- List every local dev server on your Mac, with the agent, terminal, worktree and branch that started it — so a row reads `Claude Code, in Ghostty` rather than `node`.
- Search by port, project, branch or agent.
- Open a server in the browser, copy its URL, or open its project folder.
- Stop a stray server without leaving Raycast.
