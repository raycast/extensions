{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "tidyports",
  "title": "TidyPorts",
  "description": "See which dev servers are running on your Mac, which agent started each one, and kill the strays. Requires the free TidyPorts app, which provides the CLI this reads from.",
  "icon": "command-icon.png",
  "author": "Dan_Marek",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "list-servers",
      "title": "List Dev Servers",
      "subtitle": "TidyPorts",
      "description": "Every local dev server, with the agent, terminal and worktree that started it.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.83.0",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  },
  "overrides": {
    "@types/react": "^18.3.3"
  }
}
