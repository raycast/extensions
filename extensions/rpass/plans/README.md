# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `1cf4ad9`. Execute in the order below unless dependencies say otherwise. Each executor: read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title                                                         | Priority | Effort | Depends on           | Status |
| ---- | ------------------------------------------------------------- | -------: | -----: | -------------------- | ------ |
| 001  | Restore the Raycast lint baseline                             |       P1 |      S | —                    | DONE   |
| 002  | Add contract tests for the rpass client boundary              |       P1 |      M | 001                  | DONE   |
| 003  | Preserve extra entry lines without undefined row values       |       P1 |      S | 001; 002 recommended | DONE   |
| 004  | Show actionable errors when loading the vault list fails      |       P1 |      S | 001                  | DONE   |
| 005  | Move rpass install checks out of React render                 |       P1 |      S | 001                  | DONE   |
| 006  | Prevent copying or pasting placeholder TOTP codes             |       P2 |      S | 001                  | DONE   |
| 007  | Use the safe JSON parser for list entries                     |       P2 |      S | 002                  | DONE   |
| 008  | Add a README for setup, usage, and the rpass CLI contract     |       P3 |      S | 001                  | DONE   |
| 009  | Spike Raycast write flows for future rpass commands           |       P3 |      M | 008                  | DONE   |
| 010  | Spike a reusable passphrase unlock flow                       |       P3 |      S | 002; 005             | DONE   |
| 011  | Spike a typed show-entry model instead of text round-tripping |       P3 |      S | 002; 003             | DONE   |
| 012  | Reuse GPG agent unlock state before prompting again            |       P1 |      M | —                    | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 should run first because the current lint baseline is failing; later plans should not mix behavior changes with formatting fixes.
- 002 should precede deeper `rpass-client.ts` changes because it characterizes the strict CLI boundary, including `--passphrase-stdin` and JSON error behavior.
- 003 can be done after 001, but is safer after 002 because `rpass-client` show-output behavior will be covered.
- 007 depends on 002 because it should add a focused regression test to the rpass-client contract suite.
- 009 is a design spike for future write commands and should wait until README/contract docs exist.
- 010 and 011 are refactor/design spikes; they should not be implemented until the safety tests and small correctness fixes are in place.
- 012 supersedes the old spike-only recommendation in 010 because Show Entry and Edit Entry now both have passphrase-first flows and users are seeing repeated prompts after successful unlocks.

## Verification baseline from recon

At audit time:

- `npx tsc --noEmit` exited 0.
- `npm test` exited 0 with 12 passing tests.
- `npm run lint -- --exit-on-error --non-interactive` failed because Raycast/Prettier reported `src/vault/presentation/store.tsx`, and Raycast warned that `package.json` title should be `RPass`.
- `npm audit --omit=dev --json` reported 0 production vulnerabilities.

Use these commands as the default gate for implementation plans unless a plan states otherwise:

```bash
npx tsc --noEmit
npm test
npm run lint -- --exit-on-error --non-interactive
```

## Findings covered

- Broken lint baseline: covered by plan 001.
- Untested `rpass` CLI boundary: covered by plan 002.
- Extra lines without `:` can become rows with undefined values: covered by plan 003.
- Vault list load errors are hidden behind `console.error`: covered by plan 004.
- `checkInstall()` runs during React render: covered by plan 005.
- TOTP copy/paste can use placeholder `------`: covered by plan 006.
- `listEntries()` uses direct `JSON.parse`: covered by plan 007.
- Missing README/setup documentation: covered by plan 008.
- Direction options for write commands, reusable unlock flow, and typed show-entry model: covered by plans 009-011 as spikes.
- Repeated GPG passphrase prompts after successful unlock: covered by plan 012.

## Findings considered and rejected

- Production dependency vulnerabilities: rejected for now because `npm audit --omit=dev --json` reported 0 vulnerabilities during recon.
- Shell injection through entry names: rejected for now because the extension uses `spawn(resolveExecutable(), args, ...)` with an argv array rather than string-built shell commands.
- Implementing write commands immediately: rejected for now; `AGENTS.md` lists these as CLI roadmap items, and the extension should first spike UX/contracts before adding destructive flows.
