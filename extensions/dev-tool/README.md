# ctool-custom

各种加解密、散列与文本处理工具（SM4、UUID、MD5、Base64、Hex、Hash、JWT、bcrypt、文本对比）

### 使用方式
 1. 导入扩展之前，需要在该项目的根目录执行下面的命令才可使用
 2. npm install && npm run build
 3. 如果想在本地继续开发，则运行：npm install && npm run dev


