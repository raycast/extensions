{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "delink",
  "title": "Delink",
  "description": "Quickly parse and inspect URL query parameters from the clipboard.",
  "icon": "icon.png",
  "author": "ariesly15",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Developer Tools"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "commands": [
    {
      "name": "url-parse",
      "title": "URL Parse",
      "description": "Parse and inspect URL query parameters from the clipboard",
      "mode": "view",
      "preferences": [
        {
          "name": "allowedProtocols",
          "title": "Allowed Protocols",
          "description": "Comma-separated list of URL protocols to recognize (e.g. http,https,ftp). URLs with other protocols will not be parsed.",
          "type": "textfield",
          "default": "http,https",
          "required": false
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.11"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
