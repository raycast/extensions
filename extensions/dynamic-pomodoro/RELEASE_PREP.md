# Release Prep (Raycast)

## Local checks
- npm ci
- npm run lint
- npm test
- npm run build

## Publish
- npm run publish

## Notes
- Keep `main` and `develop` in sync before tagging release.
- Create release tag from `main` (e.g. `v0.1.0`).
