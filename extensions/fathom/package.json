{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "fathom",
  "title": "Fathom",
  "description": "Search Fathom meetings, recordings, and team members",
  "icon": "extension-icon.png",
  "author": "chrismessina",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "search-meetings",
      "title": "Search Meetings",
      "description": "Search Fathom meetings",
      "mode": "view"
    },
    {
      "name": "search-team-members",
      "title": "Search Team Members",
      "description": "Search Fathom team members",
      "mode": "view"
    }
  ],
  "tools": [
    {
      "name": "list-meetings",
      "title": "List Meetings",
      "description": "Search and list Fathom meetings with full-text search across titles, summaries, and transcripts, plus filters for participants, date ranges, topics, and meeting types"
    },
    {
      "name": "get-meeting-details",
      "title": "Get Meeting Details",
      "description": "Retrieve detailed information about a specific Fathom meeting including summary, transcript, action items, participants, and duration"
    },
    {
      "name": "list-team-members",
      "title": "List Team Members",
      "description": "List Fathom team members with their names, emails, and team affiliations"
    }
  ],
  "dependencies": {
    "@chrismessina/raycast-downloader": "^0.1.5",
    "@chrismessina/raycast-kit": "^0.2.0",
    "@chrismessina/raycast-logger": "^1.5.0",
    "@raycast/api": "^2.2.1",
    "@raycast/utils": "^2.2.7"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^20.17.10",
    "@types/react": "19.1.13",
    "eslint": "^9.35.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "npm run copy-runner && ray build",
    "dev": "npm run copy-runner && ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npm run verify-runner && npx @raycast/api@latest publish",
    "copy-runner": "node -e \"const p=require('path'),f=require('fs'),s=p.join('node_modules','@chrismessina','raycast-downloader','dist','runner.bundle.js');if(!f.existsSync(s))throw new Error('Missing '+s+' — run the package build.');f.copyFileSync(s,p.join('assets','raycast-downloader-runner.js'))\"",
    "verify-runner": "node -e \"const f=require('fs'),p=require('path'),c=require('crypto');const a=p.join('assets','raycast-downloader-runner.js'),b=p.join('node_modules','@chrismessina','raycast-downloader','dist','runner.bundle.js');const h=x=>{if(!f.existsSync(x))throw new Error('Missing '+x+' \\u2014 run npm install, then npm run copy-runner.');return c.createHash('sha256').update(f.readFileSync(x)).digest('hex')};const A=h(a),B=h(b);if(A!==B){console.error('Runner asset is STALE \\u2014 it does not match the installed package.\\\\n  asset : '+A+'\\\\n  bundle: '+B+'\\\\nRun: npm run copy-runner');process.exit(1)}console.log('runner asset matches the installed bundle ('+A.slice(0,12)+')')\""
  },
  "preferences": [
    {
      "name": "fathomApiKey",
      "type": "password",
      "required": true,
      "title": "Fathom API Key",
      "description": "Your Fathom API key. Get it from https://fathom.video/settings/api"
    },
    {
      "name": "exportDirectory",
      "type": "directory",
      "required": false,
      "title": "Export Directory",
      "description": "Directory for exported summaries and transcripts",
      "default": "~/Downloads"
    },
    {
      "name": "notifyOnDownloadFinish",
      "type": "checkbox",
      "required": false,
      "title": "Download Notifications",
      "label": "Notify when a download finishes",
      "description": "Downloads keep running after you close Raycast, so the progress toast is gone by the time they finish. This posts a desktop notification instead.",
      "default": true
    },
    {
      "name": "useRaycastNotification",
      "type": "checkbox",
      "required": false,
      "title": "Notification Style",
      "label": "Send notifications through Raycast",
      "description": "Shows the notification as Raycast instead of Script Editor. Requires the separate \"Raycast Notification\" extension by maxnyby — without it, notifications are silently dropped.",
      "default": false
    },
    {
      "name": "verboseLogging",
      "title": "Verbose Logging",
      "description": "Enable detailed console logging for debugging purposes",
      "type": "checkbox",
      "label": "Enable Verbose Logging",
      "default": false,
      "required": false
    }
  ],
  "overrides": {
    "brace-expansion": "5.0.9",
    "js-yaml@4": "4.3.2"
  }
}
