# Third-party skills installed via `npx skills add mattpocock/skills`
# Files are ignored; restore them anytime with the skills CLI (skills-lock.json is committed).
.claude/skills/

# dependencies
/node_modules

# Raycast build artifacts (regenerated by `ray`)
raycast-env.d.ts
.raycast-swift-build
compiled_raycast_swift

# misc
.DS_Store
