# Third-party skills installed via `npx skills add mattpocock/skills`
# Files are ignored; restore them anytime with the skills CLI.
.claude/skills/
skills-lock.json

# Dev-only AI-assistant context — not part of the published extension.
CONTEXT.md

# dependencies
/node_modules

# Raycast build artifacts (regenerated by `ray`)
raycast-env.d.ts
.raycast-swift-build
compiled_raycast_swift

# misc
.DS_Store
