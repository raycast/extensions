{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "mic-mute",
  "title": "Mic Mute",
  "description": "Toggle microphone mute with menu bar indicator",
  "icon": "extension-icon.png",
  "author": "davidprokopec",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Media",
    "Communication"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "fallbackUnmutedVolume",
      "type": "textfield",
      "required": false,
      "title": "Default Unmute Volume",
      "description": "Volume level (0-100) to use when unmuting if no previous volume was saved",
      "default": "70"
    }
  ],
  "commands": [
    {
      "name": "toggle-mic",
      "title": "Toggle Microphone",
      "description": "Quickly toggle microphone mute on/off",
      "mode": "no-view"
    },
    {
      "name": "toggle-microphone-on-off",
      "title": "Microphone Status",
      "description": "Show microphone status in menu bar",
      "mode": "menu-bar"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.10",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}