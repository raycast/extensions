# Raycast Focus Extra

Raycast extension to show Raycast Focus sessions: table or timeline UI and “what’s done”; action to write entries to Apple Calendar.

**Cursor context**

- Use `.cursor/rules/` for conventions (Raycast extension, TypeScript, project overview).
- Use `.cursor/skills/` for Raycast extension and Apple Calendar workflows.
- Use `.cursor/commands/` for slash workflows.
- Keep `CHANGELOG.md` at repo root for extension version history; document notable changes per release so users see what changed between releases.
