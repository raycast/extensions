---
description: TypeScript and React conventions for this project
globs: "**/*.ts", "**/*.tsx"
alwaysApply: false
---

# TypeScript conventions

- Strict TS, ES2023 (per tsconfig). Prefer concision over grammar in comments and messages.
- Use React when building List/Detail/Grid UIs; functional components and hooks from `@raycast/utils` (useCachedPromise, usePromise).
