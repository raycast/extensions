# Default agent

When working on this extension:

- Prefer Raycast List/Detail and `@raycast/utils` (useCachedPromise, usePromise, runAppleScript, executeSQL).
- Check package.json for command entry points; each command’s `name` maps to a file under src/.
