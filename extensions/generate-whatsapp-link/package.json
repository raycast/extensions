{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "generate-whatsapp-link",
  "title": "Generate WhatsApp Link",
  "description": "Type or paste any phone number — local, international, or formatted — and instantly open a WhatsApp chat without saving the contact. Handles country codes, strips formatting, and copies the wa.me link to clipboard.",
  "icon": "extension-icon.png",
  "author": "laura_carta",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Communication"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "defaultCountryCode",
      "title": "Default Country Code",
      "description": "Your country calling code (digits only, no +). Used to complete local numbers that have no country code. Example: 55 for Brazil, 1 for USA/Canada, 44 for UK.",
      "type": "textfield",
      "required": true
    }
  ],
  "commands": [
    {
      "name": "generate-whatsapp-link",
      "title": "Generate WhatsApp Link",
      "description": "Open a WhatsApp chat from any phone number format. Strips spaces, dashes, and parentheses, prepends your default country code for local numbers, and opens wa.me directly.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.19",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/jest": "^30.0.0",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "jest": "^30.4.2",
    "prettier": "^3.5.3",
    "ts-jest": "^29.4.11",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish",
    "test": "jest"
  }
}
