{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "random-date-generator",
  "title": "Random Date Generator",
  "description": "Generate random dates within a specified date range",
  "icon": "icon.png",
  "author": "yasinozmeen",
  "categories": ["Productivity"],
  "license": "MIT",
  "commands": [
    {
      "name": "generate",
      "title": "Generate Random Date",
      "subtitle": "Random Date",
      "description": "Generate a random date between start and end dates",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "startDate",
      "type": "textfield",
      "required": true,
      "title": "Start Date",
      "description": "Minimum date for random generation (YYYY-MM-DD format)",
      "placeholder": "2025-01-01"
    },
    {
      "name": "endDate",
      "type": "textfield",
      "required": true,
      "title": "End Date",
      "description": "Maximum date for random generation (YYYY-MM-DD format)",
      "placeholder": "2030-12-31"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.90.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "22.9.0",
    "@types/react": "^18.3.3",
    "eslint": "^8.57.0",
    "typescript": "^5.5.3"
  },
  "scripts": {
    "build": "ray build -e dist -o dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
