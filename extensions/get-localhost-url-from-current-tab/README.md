# Get Localhost URL from Current Tab

Get the localhost URL from the current tab. Used to convert the current preview url into a localhost equivalent so we can easily open the current tab in localhost