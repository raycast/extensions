export interface Preferences {
  layout: string;
  itemSize: string;
  itemInset: string;
  createdActions: string;
  showDocument: boolean;
  showCode: boolean;
  showScript: boolean;

  fileType: string;
  saveDirectory: string;
}
