{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "github-actions",
  "title": "GitHub Actions",
  "description": "Manage GitHub Actions across repos",
  "icon": "extension-icon.png",
  "author": "weichengwu",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "githubToken",
      "type": "password",
      "title": "GitHub Personal Access Token",
      "required": true,
      "description": "Used for GitHub API requests. Prefer a fine-grained PAT with repository read access and Actions write access for rerun, cancel, and dispatch."
    }
  ],
  "commands": [
    {
      "name": "actions-hub",
      "title": "Actions Hub",
      "subtitle": "Recent runs and quick actions",
      "description": "View recent workflow runs and perform GitHub Actions quick actions",
      "mode": "view"
    },
    {
      "name": "dispatch-workflow",
      "title": "Dispatch Workflow",
      "subtitle": "Trigger workflow_dispatch",
      "description": "Trigger a GitHub Actions workflow with ref and inputs",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.13",
    "@raycast/utils": "^1.17.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
