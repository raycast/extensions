# FlowFerry Changelog

## [Initial Version] - 2026-06-10

- Save the active browser tab to your FlowFerry library (`Save Active Tab` command, no-view).
- Save an arbitrary URL via a Raycast form, with optional title override (`Save URL` command).
- Configure your account via a single `API Key` preference; generate the key at https://flowferry.app/account.
