{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "illuminate-keyboard",
  "title": "Illuminate Keyboard",
  "description": "Toggle your MacBook keyboard backlight on and off with a single command",
  "icon": "extension-icon.png",
  "author": "sandro_vol",
  "categories": ["System"],
  "license": "MIT",
  "commands": [
    {
      "name": "toggle-keyboard-light",
      "title": "Toggle Keyboard Light",
      "description": "Toggle the keyboard backlight on or off",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.60.1",
    "execa": "^8.0.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "18.18.4",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "build:native": "cd swift && swift build -c release --arch arm64 --arch x86_64 && cp .build/apple/Products/Release/keyboard ../assets/",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish",
    "typecheck": "tsc --noEmit"
  }
}
