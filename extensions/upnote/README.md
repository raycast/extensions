# UpNote

A basic raycast extension that supports creation of a note and notebook along with viewing notes tagged by a specific tag. The extension uses x-callback-url endpoints provided in the [documentation](https://help.getupnote.com/more/x-callback-url-endpoints).
