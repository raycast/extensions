# Maximize Windows

Maximize every window of the currently focused application.

## Permissions

This extension controls windows through macOS System Events, so the first time you run it you may be prompted to grant Raycast **Accessibility** access under `System Settings → Privacy & Security → Accessibility`. Enable it for the command to work.
