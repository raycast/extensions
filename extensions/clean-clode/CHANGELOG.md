## [Initial Version] - {PR_MERGE_DATE}

### Added
- Clean Text command: paste mangled Claude Code / Codex terminal output and get clean readable text, copied to clipboard automatically
- Clean Clipboard command: instantly clean clipboard contents with no UI — ideal for a hotkey
- Local history with search, copy, and delete per item
- Collapse extra spaces toggle (persisted) for narrow terminal output

### Changed
- Cleaned result now renders inline in the same view (with "Clean Another" action) instead of pushing a new screen and dropping the user back to Raycast root
