export interface Preferences {
  showBrowser: boolean;
  showEmail: boolean;
  showOther: boolean;
  showAll: boolean;
  layout: string;
  columns: string;
  itemInset: string;
  surfEngine: string;
  sortBy: string;
}
