# AIDrop Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build a Raycast extension that copies recently downloaded files through a Finder-like pasteboard helper so they can be pasted into AI apps.

**Architecture:** Create a minimal Raycast extension scaffold, keep recent-file discovery in a testable library module, and invoke a compiled Swift helper from the command UI. Verify behavior with Node tests for the loader and a successful Swift compile for the helper.

**Tech Stack:** Raycast API, React, TypeScript, Node test runner, Swift, AppKit, Foundation

---

### Task 1: Scaffold the Extension Files

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `README.md`
- Create: `.gitignore`
- Create: `src/copy-recent-downloads.tsx`
- Create: `src/lib/recent-files.ts`
- Create: `src/lib/recent-files.test.ts`
- Create: `bin/finder-copy-files.swift`

**Step 1: Write the failing test**

Create `src/lib/recent-files.test.ts` with tests for hidden-file filtering, directory filtering, descending sort order, and the maximum item count.

**Step 2: Run test to verify it fails**

Run: `npm test`
Expected: FAIL because `src/lib/recent-files.ts` and/or exported functions do not exist yet.

**Step 3: Write minimal implementation**

Create `src/lib/recent-files.ts` and implement the minimal scanning and sorting logic needed for the tests to pass. Then wire `src/copy-recent-downloads.tsx` to consume that module.

**Step 4: Run test to verify it passes**

Run: `npm test`
Expected: PASS for the recent-file loader tests.

**Step 5: Commit**

Git is unavailable in this workspace today, so skip the commit step and continue with verification.

### Task 2: Add the Native Pasteboard Helper

**Files:**
- Create: `bin/finder-copy-files.swift`
- Create: `scripts/build-helper.mjs`
- Modify: `package.json`

**Step 1: Write the failing verification**

Attempt to compile the helper before the source file or build script is complete.

**Step 2: Run verification to confirm it fails**

Run: `npm run build:helper`
Expected: FAIL until the Swift source and script are fully in place.

**Step 3: Write minimal implementation**

Add the Swift source and a small Node build script that runs `swiftc`, writes the binary to `bin/finder-copy-files`, and marks it executable.

**Step 4: Run verification to confirm it passes**

Run: `npm run build:helper`
Expected: PASS and `bin/finder-copy-files` exists.

**Step 5: Commit**

Git is unavailable in this workspace today, so skip the commit step and continue with verification.

### Task 3: Verify the Extension Project

**Files:**
- Modify: `package.json`
- Modify: `README.md`

**Step 1: Write the failing verification**

Run the extension checks before all scripts and dependencies are configured.

**Step 2: Run verification to confirm it fails**

Run: `npm run lint`
Expected: FAIL until project metadata, scripts, and source files are coherent.

**Step 3: Write minimal implementation**

Finish any remaining manifest fields, scripts, and docs so the project can be installed and run in Raycast development mode.

**Step 4: Run verification to confirm it passes**

Run: `npm test && npm run build:helper && npm run lint`
Expected: PASS.

**Step 5: Commit**

Git is unavailable in this workspace today, so skip the commit step and continue.
