# harmony

Generate colour harmonies from a base colour.

## Inputs

- `COLOUR` required: base colour.
- `HARMONY_TYPE` optional: requested harmony type, such as complementary.

## Options

- `--pretty`, `-p`: show a visual colour output.
- Global: `--json`, `--quiet`, `--output`.

## Output

Harmony colours derived from the base colour.
