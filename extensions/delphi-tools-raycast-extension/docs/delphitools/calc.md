# calc

Evaluate a mathematical expression.

## Inputs

- `EXPRESSION` optional: expression to evaluate.

## Options

- `--angles <ANGLES>`: `deg` or `rad`; default `rad`.
- Global: `--json`, `--quiet`, `--output`.

## Output

Evaluation result.
