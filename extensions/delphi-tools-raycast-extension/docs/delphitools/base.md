# base

Convert numbers between decimal, hexadecimal, octal, and binary.

## Inputs

- `VALUE` required: number to convert.
- `TARGETS` optional variadic: target bases.

## Options

- `--from <FROM>`: source base; default `auto`.
- Global: `--json`, `--quiet`, `--output`.

## Output

Number converted to target bases.
