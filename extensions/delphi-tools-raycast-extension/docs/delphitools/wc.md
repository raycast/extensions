# wc

Count words, characters, sentences, and reading time.

## Inputs

- `INPUT` optional: text, file path, or stdin.

## Options

- Global: `--json`, `--quiet`, `--output`.

## Output

Word count, character count, sentence count, and reading time.
