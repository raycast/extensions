# unit

Convert between units.

## Inputs

- `VALUE` required: value with unit, e.g. `100kg` or `100 kg`.
- `TARGETS` optional variadic: target units.

## Options

- Global: `--json`, `--quiet`, `--output`.

## Output

Converted unit values for length, weight, data, temperature, volume, speed, pressure, or time.
