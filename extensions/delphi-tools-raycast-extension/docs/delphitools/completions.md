# completions

Generate shell completions.

## Inputs

- `SHELL` required: `bash`, `elvish`, `fish`, `powershell`, or `zsh`.

## Options

- Global: `--json`, `--quiet`, `--output`.

## Output

Shell completion script.
