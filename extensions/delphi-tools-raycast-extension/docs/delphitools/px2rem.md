# px2rem

Convert pixels to rem.

## Inputs

- `VALUE` required: pixel value.

## Options

- `--base <BASE>`: base font size in pixels; default `16`.
- Global: `--json`, `--quiet`, `--output`.

## Output

Equivalent rem value.
