# Changelog

## [Initial Release] - {PR_MERGE_DATE}

### Added

- Initial release
- Rename images to `[name].[hash8].[ext]` using MD5 content hash
- Idempotent: already-hashed files are skipped
- Supports JPG, JPEG, PNG, GIF, WebP, SVG, BMP, TIFF, ICO, AVIF
