# Changelog

## [Store-ready command and metadata refresh] - {PR_MERGE_DATE}

- Added six metadata screenshots for the extension store listing.
- Made `Multi-URL` the dashboard/root command for saved sets management.
- Added `Create from Clipboard` directly inside the dashboard actions.
- Added best-effort page title suggestions for single-link clipboard imports.
- Added `Cmd + Shift + Return` in create flows to save a set and open its links immediately.
- Reduced recent run history to the latest five entries.
- Enforced one-to-one QuickURL mapping so each saved set belongs to at most one QuickURL slot at a time.
- Removed redundant standalone commands for search, saved sets, and mapping.
- Simplified command naming by removing `Multi-URL` prefixes from non-dashboard commands.
- Updated QuickURL command subtitles to `Launch instantly`.
- Added emoji support to `Create New Set` so it matches the `Edit Set` capabilities.
- Updated help and README copy to reflect the current command surface.
