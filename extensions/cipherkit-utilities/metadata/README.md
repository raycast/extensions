# Metadata

Add screenshots here for the Raycast Store:
- `1.png` - Main form view
- `2.png` - Decoded JWT result with expiry status

Screenshots should be captured from Raycast using:
1. Run `npm run dev` to load the extension
2. Open Raycast and search for "Decode JWT"
3. Take screenshots using `Cmd + Shift + 5` on macOS
