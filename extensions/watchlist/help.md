# Set Up Your Watchlist File

1. Create a Markdown file in your Obsidian vault, such as `Movies.md`.
2. Select that file in the **Movies Markdown File** field above.
3. Return to the command. The required `Watchlist` and `Watched` sections will be created automatically if needed.
