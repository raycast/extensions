{
  "name": "@palette-picker/native",
  "version": "0.0.1",
  "scripts": {
    "build:swift": "swift build --configuration=release",
    "postbuild": "cp .build/release/color-picker ../assets/color-picker",
    "build": "run-s build:swift postbuild"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
