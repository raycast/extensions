{
  "name": "@tailwind-picker/native",
  "version": "0.0.1",
  "scripts": {
    "build:swift": "swift build --configuration=release",
    "postbuild": "cp .build/release/tailwind-picker ../assets/tailwind-picker",
    "build": "run-s build:swift postbuild"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
