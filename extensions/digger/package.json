{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "digger",
  "title": "Digger",
  "description": "Like dig but for the web. Comprehensive website intelligence and metadata extraction.",
  "icon": "digger.png",
  "author": "chrismessina",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Developer Tools",
    "Data",
    "Web"
  ],
  "license": "MIT",
  "crossExtensions": [
    "pernielsentikaer/wayback-machine"
  ],
  "preferences": [
    {
      "name": "acceptLanguage",
      "title": "Page Language",
      "description": "Sent as the Accept-Language header. Sites that serve more than one language use it to choose which version Digger analyses; without it they pick for you.",
      "type": "textfield",
      "default": "en-US",
      "required": false
    },
    {
      "name": "prettyPrintCode",
      "title": "Code Formatting",
      "label": "Pretty-print JSON and XML when viewing a file",
      "description": "Re-indent minified files so they are readable. Turn off to see exactly the bytes the server sent \u2014 Copy and Download always use the original either way.",
      "type": "checkbox",
      "required": false,
      "default": true
    },
    {
      "name": "verboseLogging",
      "title": "Debug Logging",
      "description": "Show detailed logs in the console for debugging.",
      "type": "checkbox",
      "label": "Enable extra diagnostics in the console",
      "default": false,
      "required": false
    },
    {
      "name": "strictRedaction",
      "title": "Strict Redaction",
      "description": "Turn on before reproducing an issue, then share only the new lines.",
      "type": "checkbox",
      "label": "Also hide URL query strings and fragments in logs",
      "default": false,
      "required": false
    }
  ],
  "commands": [
    {
      "name": "digger",
      "title": "Digger",
      "description": "Get comprehensive website intelligence and metadata",
      "mode": "view",
      "arguments": [
        {
          "name": "url",
          "placeholder": "https://raycast.com",
          "type": "text",
          "required": true
        }
      ],
      "preferences": [
        {
          "name": "autoLoadUrlFromClipboard",
          "title": "Auto Load URL from Clipboard",
          "description": "Automatically load the URL from the clipboard when the command is executed",
          "type": "checkbox",
          "label": "Enable",
          "default": false,
          "required": false
        },
        {
          "name": "autoLoadUrlFromSelectedText",
          "title": "Auto Load URL from Selected Text",
          "description": "Automatically load the URL from the selected text when the command is executed",
          "type": "checkbox",
          "label": "Enable",
          "default": false,
          "required": false
        },
        {
          "name": "enableBrowserExtensionSupport",
          "title": "Enable Browser Extension Support",
          "description": "Automatically load the URL from the active browser tab when the command is executed",
          "type": "checkbox",
          "label": "Enable",
          "default": false,
          "required": false
        }
      ]
    }
  ],
  "dependencies": {
    "@chrismessina/raycast-logger": "^1.5.0",
    "@raycast/api": "^2.2.1",
    "@raycast/utils": "^2.2.7",
    "cheerio": "^1.2.0",
    "raycast-cross-extension": "^0.2.3"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.0",
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.39.5",
    "prettier": "^3.9.6",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "prebuild": "node scripts/cache-schema.mjs",
    "dev": "ray develop",
    "predev": "node scripts/cache-schema.mjs",
    "cache-schema": "node scripts/cache-schema.mjs",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prelint": "node scripts/cache-schema.mjs --check",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  },
  "overrides": {
    "@humanfs/node": "^0.16.8"
  }
}
