{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "french-company-search",
  "title": "French Company Search",
  "description": "Search French company information using SIREN/SIRET numbers via INPI API. Get formatted legal details ready for contracts and documents.",
  "icon": "extension-icon.png",
  "author": "fma16",
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Search French Company",
      "description": "Search company information using SIREN or SIRET number",
      "mode": "view"
    }
  ],
  "categories": [
    "Finance",
    "Productivity"
  ],
  "dependencies": {
    "@raycast/api": "^1.102.3",
    "@raycast/utils": "^2.2.0",
    "axios": "^1.7.2"
  },
  "preferences": [
    {
      "name": "inpiUsername",
      "title": "INPI Username",
      "description": "Your username (email) for the INPI API access.",
      "type": "textfield",
      "required": true
    },
    {
      "name": "inpiPassword",
      "title": "INPI Password",
      "description": "Your password for the INPI API access.",
      "type": "password",
      "required": true
    }
  ],
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/jest": "^29.5.14",
    "@types/node": "22.13.10",
    "@types/react": "18.3.3",
    "csv-parser": "^3.2.0",
    "eslint": "^9.22.0",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "overrides": {
    "@types/react": "18.3.3"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:unit": "jest --testPathIgnorePatterns=\"integration|local|performance\"",
    "test:integration": "jest --testPathPattern=\"api-mocked.integration.test.ts\" --timeout=10000",
    "test:integration:real": "jest --testPathPattern=\"api-real.local.test.ts\" --timeout=60000",
    "test:performance": "jest --testPathPattern=\"performance.test.ts\" --timeout=10000",
    "test:ci": "jest --coverage=false --silent --maxWorkers=2",
    "test:full": "jest --coverage --verbose",
    "build-greffes": "ts-node build-greffes-index.ts",
    "compress-greffes": "ts-node scripts/compress-greffes.ts"
  }
}
