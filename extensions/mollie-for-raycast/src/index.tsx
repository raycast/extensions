// This is just a fallback, not meant to be used directly.
// The main functionality is in transactionsMenuBar.tsx

import TransactionsMenuBar from "./transactionsMenuBar";

export default function Command() {
  return <TransactionsMenuBar />;
}
