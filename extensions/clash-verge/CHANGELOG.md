# Clash Verge Changelog

## [Initial Release] - {PR_MERGE_DATE}

- Add `Network` command to enable or disable `System Proxy` and `TUN Mode`.
- Add `Proxy Mode` command to switch between `rule`, `global`, and `direct`.
- Add extension preferences for socket path, system proxy host, and optional `verge.yaml` path.
- Add README note about potential temporary mismatch between runtime state and GUI state.
