/** How many index rows one query may rank. */
export const LIVE_RESULTS = 50;
/** How many of those become Raycast rows. Each row carries an action tree. */
export const LIVE_RENDERED_RESULTS = LIVE_RESULTS;
