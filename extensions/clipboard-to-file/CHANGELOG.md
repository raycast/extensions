# Clipboard to File Changelog

## [Initial Release] - {PR_MERGE_DATE}

- Save clipboard text as UTF-8 in a configurable destination folder
- Copy clipboard files to the destination folder
- Replace destination files atomically to avoid partial writes
- Default the destination folder to `/Users/Shared`
