const theme = {
  grey: '#909090',
  green: '#288056',
  red: '#802828',
};

export const config = {
  colors: {
    key: theme.grey,
    prio: {
      positive: theme.grey, // theme.green,
      negative: theme.grey, // theme.red,
    },
  },
};
