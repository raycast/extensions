/** Where users create an API key. Surfaced in onboarding and auth-failure paths. */
export const API_KEY_CONSOLE_URL = "https://console.anthropic.com/settings/keys";

/** Anthropic's plan/billing page, for quota and credit-balance failures. */
export const BILLING_CONSOLE_URL = "https://console.anthropic.com/settings/billing";

/** The extension icon, reused for empty states so they read as Claude surfaces. */
export const CLAUDE_ICON = "icon.png";
