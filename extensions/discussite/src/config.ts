/**
 * Raycast Extension Configuration
 *
 * Uses Raycast's environment API to distinguish dev from production.
 * - `ray develop` → localhost (for local development)
 * - Installed extension → production URL
 */

export const DS_BASE_URL = "https://discuss.site";
