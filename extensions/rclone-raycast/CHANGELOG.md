# rclone Changelog

## [Hide Windows Terminal window when starting daemon] - {PR_MERGE_DATE}

Set `windowsHide: true` in `start-daemon.tsx` to prevent the terminal window from appearing when starting the rclone daemon on Windows.

## [Added rclone] - 2026-01-23

Initial version
