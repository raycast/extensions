{
  "name": "atlasmind",
  "title": "AtlasMind",
  "description": "Quick-capture URLs, text, and notes locally with smart search",
  "icon": "extension-icon.png",
  "author": "charles.escat",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "save-item",
      "title": "Save to AtlasMind",
      "description": "Capture a URL, text, or note — pre-fills from clipboard",
      "mode": "view",
      "arguments": [
        {
          "name": "url",
          "type": "text",
          "required": false,
          "placeholder": "URL"
        },
        {
          "name": "title",
          "type": "text",
          "required": false,
          "placeholder": "Title"
        }
      ]
    },
    {
      "name": "save-current-tab",
      "title": "Save Current Browser Tab",
      "description": "Capture the URL of the frontmost browser tab",
      "mode": "no-view"
    },
    {
      "name": "browse",
      "title": "Browse AtlasMind",
      "description": "Search and browse saved items",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "enableScreenshotFallback",
      "type": "checkbox",
      "required": false,
      "title": "Screenshot fallback",
      "label": "Use Microlink to screenshot pages when no image is found",
      "description": "Enables remote fallback for SPAs and pages that block scraping. Free tier: ~50 req/day without an API key.",
      "default": false
    },
    {
      "name": "microlinkApiKey",
      "type": "password",
      "required": false,
      "title": "Microlink API key",
      "description": "Optional — raises the screenshot fallback rate limit. Get one at microlink.io.",
      "placeholder": "ml_xxx"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.83.1",
    "@raycast/utils": "^1.17.0",
    "fuse.js": "^7.4.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "22.5.4",
    "@types/react": "19.0.10",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "ray build --skip-types -e dist -o dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
