# Host Setup (Local or Remote Linux)

## Requirements
- Ubuntu/Debian host (local Linux or remote VPS)
- Packages: tmux, fzf, git
- Optional: mosh for roaming-friendly sessions

## Install
```bash
sudo apt-get update
sudo apt-get install -y tmux fzf git
sudo apt-get install -y mosh
```
