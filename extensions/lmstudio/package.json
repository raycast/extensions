{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "lmstudio",
  "title": "LM Studio",
  "description": "Use local AI models from LM Studio inside Raycast",
  "icon": "extension-icon.png",
  "license": "MIT",
  "commands": [
    {
      "name": "lmstudio",
      "title": "Ask local AI",
      "description": "Send input to the lmstudio server",
      "mode": "view",
      "subtitle": "Ask a question to the currently loaded model",
      "arguments": [
        {
          "name": "prompt",
          "placeholder": "Question",
          "type": "text",
          "required": true
        }
      ]
    },
    {
      "name": "start-server",
      "title": "Start LM Studio Server",
      "description": "Starts a local lmstudio server with the selected model",
      "mode": "view",
      "subtitle": "Start a LM Studio server",
      "arguments": [
        {
          "name": "model",
          "placeholder": "Model ID",
          "type": "text",
          "required": true
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "^19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "build": "ray build",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  },
  "author": "louison_gautreau",
  "platforms": [
    "Windows",
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ]
}