/** Localized descriptions for exchanger params (tooltips). English only — Raycast Store policy. */
export const paramDescriptions: Record<string, string> = {
  manual: "Manual order processing",
  verifying: "Document verification required",
  reg: "Registration required",
  cardverify: "Card verification required",
  floating: "Floating exchange rate",
};
