{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "rake",
  "title": "Rake",
  "description": "Search and run Rake tasks from your home directory, with support for task arguments.",
  "icon": "icon.png",
  "author": "kdmsnr",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "rake",
      "title": "Run Rake Task",
      "description": "Search Rake tasks in your home directory and run them with optional arguments.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^2.3.0",
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "eslint": "^10.10.0",
    "prettier": "^3.9.6",
    "typescript": "^5.8.0"
  },
  "scripts": {
    "dev": "ray develop",
    "build": "ray build -e dist -o dist",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "prepublishOnly": "node -e \"console.error('Use npm run publish to submit this extension to the Raycast Store.'); process.exit(1)\"",
    "publish": "npx @raycast/api@latest publish"
  },
  "keywords": [
    "ruby",
    "rakefile",
    "tasks",
    "automation"
  ]
}
