{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "env-keeper",
  "title": "Env Keeper",
  "description": "Manage all your projects' .env files and global shell config, right from Raycast. Local-only, keyboard-first, free.",
  "icon": "extension-icon.png",
  "author": "Mr-MJ",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Developer Tools",
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "manage-envs",
      "title": "Manage Envs",
      "subtitle": "Env Keeper",
      "description": "Open a project's .env file and manage its variables, environments, and snapshots.",
      "mode": "view"
    },
    {
      "name": "search-env-vars",
      "title": "Search Env Vars",
      "subtitle": "Env Keeper",
      "description": "Quickly search environment variables across all registered projects.",
      "mode": "view"
    },
    {
      "name": "jump-to",
      "title": "Jump to",
      "subtitle": "Env Keeper",
      "description": "Open a project, env file, profile or shell snippet by name.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@env-keeper/core": "^1.0.0",
    "@raycast/api": "^2.3.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^10.5.0",
    "prettier": "^3.8.5",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run lint && npm test && npm run build",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish",
    "test": "vitest run"
  }
}
