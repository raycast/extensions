# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

# Raycast specific files
raycast-env.d.ts
.raycast-swift-build
.swiftpm
compiled_raycast_swift
compiled_raycast_rust

# build output
/dist

# misc
.DS_Store
*.pem
*.log

# Note: package-lock.json is intentionally NOT ignored — the Raycast store
# requires it to be committed for reproducible dependency resolution.
