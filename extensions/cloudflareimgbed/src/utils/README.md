# Utils Plan

- `retry.ts`: Controls retry count and rules (non-network errors retry up to 3 times).
- `format.ts`: Builds links/Markdown/HTML snippets and formats size/time values.
- `preferences.ts`: Helper to merge Raycast preferences with form values.
- `clipboard.ts`: Detects clipboard types (image, file, URL).
