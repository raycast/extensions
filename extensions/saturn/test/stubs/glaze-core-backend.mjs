/**
 * Minimal stub for @glaze/core/backend used when bundling app modules for
 * tests — the real module only exists inside the Glaze runtime.
 */
export const logger = {
  info() {},
  warn() {},
  error() {},
  debug() {},
};
