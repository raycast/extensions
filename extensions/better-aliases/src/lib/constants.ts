import type { Keyboard } from "@raycast/api";

export const KEYBOARD_SHORTCUTS = {
  COPY_VALUE: { modifiers: ["cmd"], key: "c" } as Keyboard.Shortcut,
  VIEW: { modifiers: ["cmd"], key: "v" } as Keyboard.Shortcut,
};
