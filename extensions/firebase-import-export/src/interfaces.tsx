/* Defining the interface for the Preferences object. */
export interface Preferences {
  firebaseAuth: string;
  exportFileStorage: string;
  collectionList: string;
  fileByCollection: boolean;
  limitDocumentExport: string;
}
