// Minimal mock of @raycast/api for unit tests (pure-function coverage only).
// The async fetch/create/update/delete functions are NOT tested here.
export const environment = {
  assetsPath: "/tmp/raycast-contacts-test-assets",
};
