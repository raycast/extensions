Capture Raycast metadata screenshots into this folder before publishing.

Recommended filenames:

- `clipboard-qr-decoder-1.png`
- `clipboard-qr-decoder-2.png`
- `clipboard-qr-decoder-3.png`

Suggested shots:

1. Loading state while scanning a clipboard image.
2. Success state with a decoded value.
3. Error state showing recovery guidance.

Use Raycast Window Capture with "Save to Metadata" enabled so the exported screenshots match Store requirements.
