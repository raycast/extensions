# Synap Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Connect Raycast to a Synap data pod (Cloud, CLI, or Hub key)
- Ask, search, and capture into the pod; writes queue as reviewable proposals
- AI tools (`@synap`) for recall, capture, packs vs runnable actions, views, and `/open` links
- Menu bar for pending tasks and proposals
