/**
 * Raycast door onto the portable locator. Implementation lives in
 * `@synap/hub-rest-client` so CLI / MCP / Raycast emit the same URL.
 */
export { openAppUrl, openPath, openUrl } from "@synap/hub-rest-client";
