# Icon Notice

`icon.svg` is a locally generated Cloudflare-style icon for this Raycast utility. `extension-icon.png` is the exported 512x512 Raycast icon referenced by `package.json`.

It intentionally uses a cloud/upload metaphor and orange palette without copying the official Cloudflare logo mark.
