# Spark Plug Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Browse worktrees under a configurable root folder
- Start a named Claude Code session (`claude -n "<name>"`) with the New Session action
- Resume any prior session from the Continue submenu, with custom title or first-message preview and a relative timestamp
- `● live` indicator on sessions whose Claude process is currently running
- Delete a session's transcript from the Delete submenu, with a confirmation prompt
- Open the worktree directly in Finder
