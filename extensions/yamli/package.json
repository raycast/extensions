{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "yamli",
  "title": "Yamli",
  "description": "Convert English-Arabic transliteration to Arabic text using Yamli",
  "icon": "extension_icon.png",
  "author": "menisy",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "yamli",
      "title": "Yamli",
      "subtitle": "English to Arabic Transliteration",
      "description": "Convert English-Arabic transliteration to Arabic text using Yamli",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "display_number_shortcuts",
      "title": "Display Number Shortcuts",
      "label": "Display (⌘ 1, ⌘ 2, etc.) next to suggestions",
      "description": "Display (⌘ 1, ⌘ 2, etc.) next to suggestions",
      "type": "checkbox",
      "required": false,
      "default": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.91.2",
    "@raycast/utils": "^1.17.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "20.8.10",
    "@types/node-fetch": "^2.6.12",
    "@types/react": "18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
