{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "snapstream",
  "title": "SnapStream",
  "description": "Live TV in Picture-in-Picture — breaking news and sports floating over your workspace",
  "icon": "extension-icon.png",
  "author": "amanti_melkamu",
  "categories": [
    "Media",
    "Productivity"
  ],
  "platforms": [
    "macOS"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "playlistUrl",
      "title": "Remote M3U Playlist URL",
      "description": "URL to a remote M3U playlist for additional channels (leave empty to disable)",
      "type": "textfield",
      "required": false,
      "default": "https://tvpass.org/playlist/m3u",
      "placeholder": "https://example.com/playlist.m3u"
    },
    {
      "name": "autoPip",
      "label": "Auto Picture-in-Picture",
      "description": "Automatically enter Picture-in-Picture mode after opening a stream",
      "type": "checkbox",
      "required": false,
      "default": true
    },
    {
      "name": "pipDelay",
      "title": "PiP Delay (seconds)",
      "description": "Seconds to wait before triggering PiP (increase if streams take longer to load)",
      "type": "textfield",
      "required": false,
      "default": "3",
      "placeholder": "3"
    }
  ],
  "commands": [
    {
      "name": "search-channels",
      "title": "Search Channels",
      "subtitle": "SnapStream",
      "description": "Browse and search live TV channels, open in Picture-in-Picture",
      "mode": "view"
    },
    {
      "name": "import-channels",
      "title": "Import Channels",
      "subtitle": "SnapStream",
      "description": "Import channels from an M3U playlist file into your channel list",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.93.2",
    "@raycast/utils": "^1.19.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.1.1",
    "@types/node": "22.13.14",
    "@types/react": "19.0.12",
    "eslint": "^9.23.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\n\nIt seems like you are trying to publish the Raycast extension to npm.\n\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\n\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
