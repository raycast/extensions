# Note about package-lock.json

After running `npm install`, a `package-lock.json` file will be generated here.
This file locks the exact versions of all dependencies and should be committed to version control.

The `.gitignore` already excludes `node_modules/` but includes `package-lock.json`.


