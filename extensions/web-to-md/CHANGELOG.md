# Web to Markdown Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Convert the active browser tab or any URL to clean Markdown
- Save to a file, copy to the clipboard, or preview inside Raycast
- Local extraction with Mozilla Readability and Turndown — nothing leaves your machine by default
- Optional external reader fallback when local extraction fails
- Configurable output directory, filename style, and frontmatter
