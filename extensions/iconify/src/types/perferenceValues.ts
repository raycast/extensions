enum primaryActionEnum {
  paste = 'paste',
  copy = 'copy',
  pasteName = 'pasteName',
  copyName = 'copyName',
  pasteFile = 'pasteFile',
  copyFile = 'copyFile',
  copyURL = 'copyURL',
}

enum iconColorEnum {
  default = 'currentColor',
  white = '#ffffff',
}

export { primaryActionEnum, iconColorEnum };
