enum primaryActionEnum {
  paste = 'paste',
  copy = 'copy',
  pasteName = 'pasteName',
  copyFile = 'copyFile',
}

enum iconColorEnum {
  default = 'currentColor',
  white = '#ffffff',
}

export { primaryActionEnum, iconColorEnum };
