# AI Hook Notifier Changelog

## [Initial Version]

- Added `Manage Hook Sounds` to configure semantic `Needs Input` and `Done` sounds
- Added `Notify AI Event` as the internal callback command for hook bridges
- Bundled default `Claude Ready To Work` and `Claude Jobs Done` sounds
- Added Claude and Gemini hook bridge support
