# Scoop App Launcher

Adding Scoop directories to the root applications in Raycast pollutes the search results and is difficult to figure out which version to launch.

This extension pulls the executable from the current version installed by Scoop and launches with the correct arguments. You can also run the apps as administrator if needed.
