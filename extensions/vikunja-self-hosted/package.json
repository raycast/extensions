{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "vikunja-self-hosted",
  "title": "Vikunja Self-Hosted",
  "description": "Manage tasks from your self-hosted Vikunja instance.",
  "icon": "extension-icon.png",
  "author": "kunal_hotwani",
  "license": "MIT",
  "preferences": [
    {
      "name": "baseUrl",
      "title": "Base URL",
      "description": "Your Vikunja base URL. The client appends /api/v1 automatically.",
      "type": "textfield",
      "required": true
    },
    {
      "name": "apiToken",
      "title": "API Token",
      "description": "Vikunja API token sent as Authorization: Bearer <token>.",
      "type": "password",
      "required": true
    }
  ],
  "commands": [
    {
      "name": "tasks",
      "title": "Tasks",
      "description": "Browse Vikunja tasks with remembered views, editing, and sorting",
      "icon": "extension-icon.png",
      "mode": "view"
    },
    {
      "name": "create-task",
      "title": "Create Task",
      "description": "Create a Vikunja task with remembered project and priority",
      "icon": "extension-icon.png",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.100.3",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
