import {Color, Grid} from "@raycast/api";

// noinspection JSUnusedGlobalSymbols
export default function CommandList() {
  return <Grid>
    <Grid.Item content={{color: Color.Blue}}></Grid.Item>
  </Grid>
}