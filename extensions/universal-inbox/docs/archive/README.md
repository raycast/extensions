# Archive

Historical docs — never auto-loaded. Load only when explicitly requested.

Move docs here when: feature no longer exists, planning doc fully executed, POC superseded by actual implementation.
