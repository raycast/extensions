# Weights & Biases Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Browse your W&B entities, projects, and runs
- Multi-account support — entities from every account in one dropdown, grouped by account
- GUI sign-in (wandb.ai/authorize) plus automatic key detection from `~/.netrc` and `*WANDB_API_KEY` env vars
- Project rows show visibility, run count, and last-active time
- Run drill-in with status filter, colored status tags, and infinite scroll
