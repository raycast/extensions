// Este arquivo contém exemplos de como usar o serviço Gemini
// Está comentado para evitar erros de lint sobre código não utilizado

// Código incorreto que estava sendo usado (para referência)
/*
import OpenAI from "openai";

const openai = new OpenAI({
  apiKey: "GEMINI_API_KEY",
  baseURL: "https://generativelanguage.googleapis.com/v1beta/openai/"
});
*/
