# Conductor

Manage Conductor with Raycast

## Requirements

- macOS with the Conductor app installed.
- Raycast needs Full Disk Access to read Conductor's local database (`~/Library/Application Support/com.conductor.app/conductor.db`).
