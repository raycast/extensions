{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "dev-port-manager",
  "title": "Dev Port Manager",
  "description": "Manage development server processes and Docker containers. View running ports, detect conflicts, and stop processes with one click. Supports Colima, Docker Desktop, Podman, and Rancher Desktop.",
  "icon": "command-icon.png",
  "author": "agent-y",
  "license": "MIT",
  "categories": ["Developer Tools", "Productivity"],
  "keywords": ["port", "process", "docker", "container", "kill", "server", "development", "colima", "podman"],
  "commands": [
    {
      "name": "list-ports",
      "title": "List Dev Ports",
      "description": "Show running development servers and Docker containers",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "ignoredProcesses",
      "title": "Ignored Processes",
      "description": "Comma-separated list of process names to ignore (e.g., nginx, postgres)",
      "type": "textfield",
      "required": false,
      "default": ""
    },
    {
      "name": "customDockerSocket",
      "title": "Custom Docker Socket Path",
      "description": "Path to Docker socket (leave empty for auto-detection)",
      "type": "textfield",
      "required": false,
      "default": ""
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.64.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.8",
    "@types/node": "20.10.0",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
