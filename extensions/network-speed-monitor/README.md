# Network Speed Monitor

Raycast extension that shows real-time network speed (download/upload) in the menu bar. Uses `netstat` to read interface stats and computes speed from 1s deltas.

**Install:** Clone repo → `npm install` → Import extension in Raycast (or `npm run dev` for development).

**Requirements:** macOS, Raycast, Node.js 16+.

MIT
