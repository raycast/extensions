# Changelog

## [Initial Release] - 2026-05-27

### Added

- Save clipboard image to `/tmp/clip-<timestamp>.png` via AppleScript (unique per invocation, no overwrite)
- Type file path into focused window
- Background sync to remote SSH hosts via `scp`
- Comma-separated multi-host support
