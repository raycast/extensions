{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "nepali-calendar",
  "title": "Nepali Calendar",
  "description": "View and navigate the Nepali calendar (Bikram Sambat) with support for both English and Nepali languages. Customize your view by setting the first day of the week.",
  "icon": "extension-icon.png",
  "author": "adarshdoesntcode",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Nepali Calendar",
      "description": "View and navigate the Nepali month calendar",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "weekStart",
      "type": "dropdown",
      "required": true,
      "title": "First Day of Week",
      "data": [
        {
          "title": "Monday",
          "value": "1"
        },
        {
          "title": "Sunday",
          "value": "0"
        }
      ],
      "description": "First Day of Week",
      "default": "0"
    },
    {
      "name": "language",
      "type": "dropdown",
      "required": true,
      "title": "Language",
      "data": [
        {
          "title": "Nepali",
          "value": "np"
        },
        {
          "title": "English",
          "value": "en"
        }
      ],
      "description": "Calendar Language",
      "default": "en"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.1",
    "@raycast/utils": "^1.17.0",
    "nepali-date-converter": "^3.4.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
