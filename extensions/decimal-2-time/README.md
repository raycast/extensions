# Decimal 2 Time

Convert between decimal hours and HH:MM:SS time format.

## Usage

- Type a **decimal value** (e.g., `3.4322`) to convert to time → `03:25:56`
- Type a **time value** (e.g., `01:30:00`) to convert to decimal → `1.5`

Press Enter to copy the result to your clipboard.
