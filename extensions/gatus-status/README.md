# Gatus Status

View the status of endpoints monitored by your Gatus instance.

## Setup

1. Open the extension preferences.
2. Set **Gatus Base URL** to your instance, for example `https://status.twin.sh`.
3. If the instance requires authentication, set **Bearer Token**.
