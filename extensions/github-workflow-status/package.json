{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "github-workflow-status",
  "title": "GitHub Workflow Status",
  "description": "Shows your recent GitHub workflow runs in your menubar",
  "icon": "extension-icon.png",
  "author": "marckohlbrugge",
  "license": "MIT",
  "commands": [
    {
      "name": "show-recent-runs",
      "title": "Show Workflow Status",
      "description": "Shows the status of your recent GitHub workflow runs in the menubar",
      "mode": "menu-bar",
      "interval": "10m",
      "label": "Workflow Status",
      "refreshTime": "10m"
    }
  ],
  "preferences": [
    {
      "name": "showAccount",
      "type": "checkbox",
      "required": false,
      "description": "Show the account name in repository paths",
      "default": false,
      "label": "Show Account Name"
    },
    {
      "name": "historyHours",
      "type": "textfield",
      "required": false,
      "title": "History (hours)",
      "description": "Show workflow runs from the last X hours",
      "default": "24"
    }
  ],
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "@raycast/api": "^1.86.1",
    "@raycast/utils": "^1.17.0",
    "date-fns": "^2.30.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "20.8.10",
    "@types/react": "18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
