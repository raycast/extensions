{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "runpool",
  "title": "Runpool",
  "description": "Manage on-demand self-hosted GitHub Actions runner pools on your Mac",
  "icon": "icon.png",
  "author": "aic",
  "categories": [
    "Developer Tools"
  ],
  "platforms": [
    "macOS"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "pools",
      "title": "Runner Pools",
      "subtitle": "runpool",
      "description": "See every runner pool, start and stop them, and change how many runners each has",
      "mode": "view"
    },
    {
      "name": "toggle",
      "title": "Toggle Local CI",
      "subtitle": "runpool",
      "description": "Pause every pool, or resume on-demand running",
      "mode": "no-view"
    },
    {
      "name": "status",
      "title": "Pool Status",
      "subtitle": "runpool",
      "description": "Keep a live summary of the pools in this command's subtitle",
      "mode": "no-view",
      "interval": "1m"
    }
  ],
  "preferences": [
    {
      "name": "runpoolPath",
      "title": "runpool Executable",
      "description": "Only needed if runpool is installed somewhere unusual. Left empty, the extension asks the system where it is.",
      "type": "textfield",
      "required": false
    }
  ],
  "tools": [
    {
      "name": "get-status",
      "title": "Get Pool Status",
      "description": "Read every runner pool: how many runners each has, how many are running, how many jobs are in flight, and what GitHub sees"
    },
    {
      "name": "start-pool",
      "title": "Start Pool",
      "description": "Bring a pool's runners online immediately, rather than waiting for a queued job to wake it"
    },
    {
      "name": "stop-pool",
      "title": "Stop Pool",
      "description": "Stand a pool's runners down. Refuses while a job is running, because stopping mid-job fails it"
    },
    {
      "name": "set-pool-count",
      "title": "Set Runner Count",
      "description": "Change how many runners a pool has. Growing registers new runners with GitHub, shrinking deregisters them"
    },
    {
      "name": "toggle-pause",
      "title": "Pause or Resume Local CI",
      "description": "Pause stands every pool down and stops them waking. Resume returns to on-demand"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.25",
    "@raycast/utils": "^2.3.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "evals": "ray evals",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  }
}
