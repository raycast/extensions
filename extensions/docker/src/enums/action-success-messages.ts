export enum ActionContainerSuccessMessages {
  start = "Container successfully started",
  stop = "Container successfully stopped",
  restart = "Container successfully restarted",
}

export enum ActionProjectSuccessMessages {
  start = "Project successfully started",
  stop = "Project successfully stopped",
  restart = "Project successfully restarted",
}
