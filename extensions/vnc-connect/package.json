{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "vnc-connect",
  "title": "VNC Connect",
  "description": "Quickly connect to VNC servers saved in macOS Screen Sharing. Add, edit, and manage custom VNC connections with saved credentials.",
  "icon": "icon.png",
  "author": "andreisuslov",
  "categories": ["Applications", "Productivity"],
  "license": "MIT",
  "version": "1.0.0",
  "commands": [
    {
      "name": "index",
      "title": "Connect to VNC",
      "description": "List and connect to saved VNC locations",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.1",
    "plist": "^3.1.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^22.17.2",
    "@types/plist": "^3.0.5",
    "@types/react": "^19.1.10",
    "eslint": "^9.33.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
