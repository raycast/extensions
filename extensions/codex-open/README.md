# Codex Open

Raycast extension for searching projects saved by the VS Code Project Manager extension and opening them in Codex.

The extension bundles its own `codex-open` script in `assets/codex-open`, so it does not depend on a user-specific executable path.
