import { Color } from "@raycast/api";

export const maxLevel = 20;

export const colorMap = {
  Red: Color.Red,
  Green: Color.Green,
  Blue: Color.Blue,
  Yellow: Color.Yellow,
} as { [key: string]: Color };
