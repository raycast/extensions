/**
 * Legacy single-database credentials. No longer used; credentials are stored
 * per-database in the registry (Manage Databases). This file is kept only
 * for reference; migration in databases.ts reads the old LocalStorage key.
 */
