{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "ke-otp",
  "title": "Ke-otp",
  "description": "Generate OTP codes with custom prefix and automatic pasting",
  "icon": "extension-icon.png",
  "author": "juinjonnchan",
  "license": "MIT",
  "commands": [
    {
      "name": "opt",
      "title": "Generate Otp",
      "description": "Generate and paste OTP code with custom prefix",
      "mode": "no-view"
    },
    {
      "name": "opt-setting",
      "title": "Otp Settings",
      "description": "Configure OTP authentication URL and custom prefix",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "otpAuthUrl",
      "title": "OTP Auth URL",
      "description": "Your OTP authentication URL (otpauth://...)",
      "type": "textfield",
      "required": false,
      "default": ""
    },
    {
      "name": "prefix",
      "title": "Custom Prefix",
      "description": "Custom prefix to add before OTP code",
      "type": "textfield",
      "required": false,
      "default": ""
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.101.1",
    "@raycast/utils": "^1.17.0",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
