meta {
  name: Delete an Entry
  type: http
  seq: 6
}

delete {
  url: {{domain}}/entries/39054946
  body: json
  auth: inherit
}

headers {
  X-NokoToken: {{x_noko_token}}
}

settings {
  encodeUrl: true
}
