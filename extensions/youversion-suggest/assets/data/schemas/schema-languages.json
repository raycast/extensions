{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Details for all languages supported by YouVersion Suggest",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "description": "The ISO 639-3 code for this language",
        "type": "string",
        "pattern": "^[a-z]{3}(_[a-z]{2,3})?$"
      },
      "name": {
        "description": "The name of this language as written in said language",
        "type": "string"
      }
    },
    "required": ["id", "name"],
    "additionalProperties": false
  }
}
