{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "notion-workers",
  "description": "Run, configure and view logs from your Notion Workers",
  "license": "MIT",
  "scripts": {
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "build": "ray build",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  },
  "dependencies": {
    "@raycast/api": "^1.104.18",
    "@raycast/utils": "^2.2.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "icon": "extension-icon.png",
  "commands": [
    {
      "mode": "view",
      "name": "notion-workers",
      "title": "Notion Workers",
      "description": "Run, configure, and view logs from your Notion Workers"
    }
  ],
  "preferences": [
    {
      "name": "workersLocation",
      "title": "Workers Location",
      "description": "Local directory where you keep your Notion workers. Used as the starting point when picking a destination for Pull Env.",
      "type": "directory",
      "required": false
    }
  ],
  "title": "Notion Workers",
  "author": "alexs",
  "platforms": [
    "Windows",
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ]
}
