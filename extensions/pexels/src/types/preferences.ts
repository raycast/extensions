export interface Preferences {
  apikey: string;
  layout: string;
  columns: string;
  applyTo: string;
  downloadSize: string;
  downloadDirectory: string;
  autoDownload: boolean;
  rememberTag: boolean;
}
