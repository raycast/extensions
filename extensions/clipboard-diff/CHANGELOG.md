# Clipboard Diff Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Diff the two most recent clipboard history entries in VS Code via `code --diff`
- Preference for the editor CLI (`code`, `code-insiders`, `cursor`, or an absolute path)
- Preference for the temp file extension, to control syntax highlighting in the diff
