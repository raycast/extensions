{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "relay",
  "title": "Relay",
  "description": "Upload recent clipboard files over SSH and copy their remote paths.",
  "icon": "extension-icon.png",
  "author": "genna_cervantes",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "keywords": [
    "ssh",
    "upload",
    "clipboard",
    "remote"
  ],
  "commands": [
    {
      "name": "upload-clipboard-file",
      "title": "Upload Clipboard File",
      "subtitle": "Relay",
      "description": "Upload a recent clipboard file over SSH and copy its remote path.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.23"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^4.1.10"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "test": "vitest run",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  },
  "preferences": [
    {
      "name": "remoteHost",
      "title": "SSH Host",
      "description": "An SSH host, user@host destination, or alias from ~/.ssh/config",
      "type": "textfield",
      "required": true,
      "placeholder": "user@example.com or my-server"
    },
    {
      "name": "remoteDirectory",
      "title": "Destination Directory",
      "description": "An absolute path or a path relative to the remote home directory",
      "type": "textfield",
      "required": true,
      "default": "~/relay/uploads",
      "placeholder": "~/relay/uploads"
    }
  ]
}
