repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: trailing-whitespace

# Run `npm run lint && npm run build` manually before committing
# The ray CLI has PTY issues with prek hooks
