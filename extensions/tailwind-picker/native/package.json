{
  "name": "@tailwind-picker/native",
  "version": "0.0.1",
  "scripts": {
    "build:swift": "swift build --configuration=release --arch arm64 --arch x86_64",
    "postbuild": "mv .build/apple/Products/Release/tailwind-picker ../assets/tailwind-picker",
    "build": "run-s build:swift postbuild"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
