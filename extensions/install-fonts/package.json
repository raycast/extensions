{
  "name": "install-fonts",
  "title": "Install Fonts",
  "description": "Install fonts from Finder selections, folders, or zip files.",
  "version": "0.1.0",
  "license": "MIT",
  "author": "will_thresher",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Design Tools"
  ],
  "icon": "icon.png",
  "commands": [
    {
      "name": "install-fonts",
      "title": "Install Fonts",
      "description": "Install selected fonts into your Mac user Fonts folder.",
      "mode": "view",
      "keywords": [
        "font",
        "fonts",
        "typeface",
        "install",
        "zip",
        "ttf",
        "otf"
      ],
      "preferences": [
        {
          "name": "trashSourceAfterInstall",
          "title": "Clean Up",
          "label": "Trash source after install",
          "description": "Move the original font file, folder, or zip to the Trash after a successful install.",
          "type": "checkbox",
          "required": false,
          "default": true
        },
        {
          "name": "duplicateHandling",
          "title": "Duplicates",
          "description": "Choose how Install Fonts should handle font files with the same filename as an existing font.",
          "type": "dropdown",
          "required": false,
          "default": "ask",
          "data": [
            {
              "title": "Ask every time",
              "value": "ask"
            },
            {
              "title": "Skip duplicates",
              "value": "skip"
            },
            {
              "title": "Overwrite duplicates",
              "value": "overwrite"
            },
            {
              "title": "Keep both",
              "value": "keep-both"
            }
          ]
        },
        {
          "name": "showFinderAfterInstall",
          "title": "Post Install",
          "label": "Show Fonts folder after install",
          "description": "Reveal your user Fonts folder in Finder after a successful install.",
          "type": "checkbox",
          "required": false,
          "default": false
        },
        {
          "name": "fontSelectionMode",
          "title": "Font Versions",
          "description": "Choose whether Install Fonts installs every discovered font or asks you to pick variants when multiple versions are found.",
          "type": "dropdown",
          "required": false,
          "default": "ask-when-variants-found",
          "data": [
            {
              "title": "Install all fonts",
              "value": "install-all"
            },
            {
              "title": "Ask when variants are found",
              "value": "ask-when-variants-found"
            }
          ]
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.100.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.10",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray dev",
    "lint": "ray lint"
  }
}
