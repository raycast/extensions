// const supportedLanguages = require("../src/supportedLanguages");

// const map = Object.values(supportedLanguages).reduce((acc, t) => {
//   acc[t.code] = {
//     ...t,
//   };
//   return acc;
// }, {});

// console.log(JSON.stringify(map));
