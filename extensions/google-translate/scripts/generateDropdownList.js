// const supportedLanguages = require("../src/supportedLanguages");

// const values = Object.values(supportedLanguages).map((t) => {
//   return {
//     title: `${t.flag ? `${t.flag} ` : ""}${t.name}`,
//     value: t.code,
//   };
// });

// console.warn(values)

// console.log(JSON.stringify(values))
