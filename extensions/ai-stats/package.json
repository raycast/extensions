{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "ai-stats",
  "title": "AI Stats",
  "description": "AI model stats via Supabase",
  "version": "1.0.0",
  "icon": "extension_icon.png",
  "author": "JonathanRReed",
  "license": "MIT",
  "categories": ["Developer Tools", "Data"],
  "engines": {
    "node": ">=22.14.0"
  },
  "commands": [
    {
      "name": "view",
      "icon": "extension_icon.png",
      "title": "View AI Stats",
      "subtitle": "AI Benchmarks & Pricing",
      "description": "View AI models from Supabase: search and leaderboards",
      "mode": "view",
      "keywords": ["ai", "models", "benchmark", "search", "leaderboard"]
    }
  ],
  "preferences": [
    {
      "name": "SHOW_PINNED_SECTION",
      "type": "checkbox",
      "title": "Show Pinned Section",
      "label": "Show Pinned Section",
      "required": false,
      "default": true,
      "description": "If enabled, shows the Pinned section at the top of Search"
    },
    {
      "name": "SUPABASE_URL",
      "type": "textfield",
      "title": "Supabase URL (optional)",
      "placeholder": "https://YOUR-PROJECT.supabase.co",
      "required": false,
      "default": "",
      "description": "Optional override for the read-only Supabase instance. Leave empty to use the default."
    },
    {
      "name": "SUPABASE_ANON_KEY",
      "type": "password",
      "title": "Supabase Anon Key (optional)",
      "required": false,
      "default": "",
      "description": "Optional override for the read-only anon key. Leave empty to use the default."
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.102.6",
    "@raycast/utils": "^1.19.1",
    "@supabase/supabase-js": "^2.57.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "^18.3.5",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\n\nIt seems like you are trying to publish the Raycast extension to npm.\n\nIf you did intend to publish it to npm, remove the `prepublishOnly` script and rerun `npm publish` again.\nIf you wanted to publish it to the Raycast Store instead, use `npm run publish` instead.\n\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
