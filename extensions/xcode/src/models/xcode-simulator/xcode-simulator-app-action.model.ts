/**
 * A Xcode Simulator App Action
 */
export enum XcodeSimulatorAppAction {
  /**
   * Launch an app
   */
  launch = "launch",
  /**
   * Terminate an app
   */
  terminate = "terminate",
  /**
   * Uninstall an app
   */
  uninstall = "uninstall",
}
