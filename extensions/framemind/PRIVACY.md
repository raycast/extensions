# Privacy

The extension invokes the signed command bridge embedded in the local FrameMind
app. It has no analytics, network client, or upload code. Queries and OCR remain
on this Mac. Capture and Ask Screen require review in FrameMind. Copy OCR writes
to the clipboard only when the user explicitly runs that command.
