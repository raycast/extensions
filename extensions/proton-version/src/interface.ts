export type Version = {
  version: string;
  commit: string;
  branch: string;
  date: string;
  mode: string;
};

export type ProtonProduct = "Proton Mail" | "Proton Drive" | "Proton Calendar" | "Proton Account";
