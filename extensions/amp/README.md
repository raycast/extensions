# Amp

Create and track [Amp](https://ampcode.com) threads from Raycast. Threads run remotely in Amp Orbs.

## Setup

Install the [Amp CLI](https://ampcode.com/manual#getting-started) and log in:

```sh
amp login
```

The extension auto-detects `amp`; set an explicit path in preferences if needed. Window capture requires Screen Recording permission for Raycast (System Settings → Privacy & Security).

## Commands

- **New Thread** — write a prompt, pick a project, mode, and visibility, attach files or a captured window, and start a thread
- **Capture Window and Start Thread** — screenshot the frontmost window and attach it to a new thread; assign a global hotkey for one-keystroke capture
- **My Threads** — browse all threads: open in browser, copy URL, change visibility, copy attach command, open in terminal
- **Amp Threads** — root-search overview with live running/unread counts
- **Amp Thread Status** — menu bar icon with running and unread counts
