{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "apple-container",
  "title": "Apple Container",
  "description": "Manage Linux containers on macOS using Apple's native container runtime. List, run, stop, shell into containers, stream logs, and monitor stacks from the menu bar — no Docker Desktop required.",
  "icon": "extension-icon.png",
  "author": "ojowwalker77",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "containers",
      "title": "Containers",
      "subtitle": "Apple Container",
      "description": "List and manage running containers",
      "mode": "view",
      "keywords": ["docker", "container", "apple", "list", "ps"]
    },
    {
      "name": "images",
      "title": "Images",
      "subtitle": "Apple Container",
      "description": "List and manage container images",
      "mode": "view",
      "keywords": ["docker", "image", "pull"]
    },
    {
      "name": "run-container",
      "title": "Run Container",
      "subtitle": "Apple Container",
      "description": "Run a new container from an image",
      "mode": "view",
      "keywords": ["docker", "run", "start", "launch", "create"]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.8",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}