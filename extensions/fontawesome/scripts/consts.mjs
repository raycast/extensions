import * as url from 'url';

export const folders = ['brands', 'duotone', 'light', 'regular', 'sharp-solid', 'solid', 'thin'];
export const repoPath = url.fileURLToPath(new URL('../', import.meta.url));
