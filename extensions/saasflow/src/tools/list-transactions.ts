// AUTO-GENERATED by apps/raycast/scripts/generate-tools.ts.
// Don't edit by hand — re-run `yarn workspace saasflow-raycast generate-tools`
// (also runs automatically before `dev` and `build`).
import { runEndpoint } from "../lib/runEndpoint.js";

type Input = {
    /** SaaSFlow company id (UUID). Omit to use the currently active company. */
    companyId?: string;
    /** Number of transactions to return (1-500, default 100). */
    limit?: number;
    /** Number of transactions to skip (default 0). */
    offset?: number;
    /** Filter by account ID. */
    accountId?: string;
};

export default async function listTransactions(input: Input): Promise<Record<string, unknown>> {
    const { companyId, limit, offset, accountId } = input;

    return runEndpoint({
        method: "GET",
        pathTemplate: "/companies/{companyId}/transactions",
        companyId,
        query: { limit, offset, accountId },
    });
}
