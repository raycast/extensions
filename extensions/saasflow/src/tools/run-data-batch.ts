// AUTO-GENERATED by apps/raycast/scripts/generate-tools.ts.
// Don't edit by hand — re-run `yarn workspace saasflow-raycast generate-tools`
// (also runs automatically before `dev` and `build`).
import { runEndpoint } from "../lib/runEndpoint.js";

type Input = {
    /** SaaSFlow company id (UUID). Omit to use the currently active company. */
    companyId?: string;
    /** JSON-encoded value for `slices` (the underlying schema is an array of objects, which Raycast's schema extractor can't model). */
    slicesJson: string;
    timeZone?: string;
};

export default async function runDataBatch(input: Input): Promise<Record<string, unknown>> {
    const { companyId, slicesJson, timeZone } = input;
    let __slicesValue: unknown;
    try {
        __slicesValue = JSON.parse(slicesJson);
    } catch (e) {
        throw new Error(`Invalid ${"slicesJson"}: ${(e as Error).message}`);
    }
    return runEndpoint({
        method: "POST",
        pathTemplate: "/companies/{companyId}/data/batch",
        companyId,
        body: { slices: __slicesValue, timeZone },
    });
}
