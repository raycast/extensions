// AUTO-GENERATED by apps/raycast/scripts/generate-tools.ts.
// Don't edit by hand — re-run `yarn workspace saasflow-raycast generate-tools`
// (also runs automatically before `dev` and `build`).
import { runEndpoint } from "../lib/runEndpoint.js";

type Input = {
    /** SaaSFlow company id (UUID). Omit to use the currently active company. */
    companyId?: string;
    q?: string;
    limit?: number;
    offset?: number;
    onlyActive?: "0" | "1" | "true" | "false";
};

export default async function listCustomers(input: Input): Promise<Record<string, unknown>> {
    const { companyId, q, limit, offset, onlyActive } = input;

    return runEndpoint({
        method: "GET",
        pathTemplate: "/companies/{companyId}/customers",
        companyId,
        query: { q, limit, offset, onlyActive },
    });
}
