{
    "$schema": "https://www.raycast.com/schemas/extension.json",
    "name": "rembg",
    "title": "RemBg - Remove Background",
    "description": "Remove backgrounds from images using rembg. Select images in Finder and remove their backgrounds instantly.",
    "icon": "extension-icon.png",
    "author": "julesmellot",
    "categories": [
        "Design Tools",
        "Productivity"
    ],
    "license": "MIT",
    "commands": [
        {
            "name": "remove-background",
            "title": "Remove Background",
            "subtitle": "RemBg",
            "description": "Remove background from selected images in Finder",
            "mode": "no-view"
        },
        {
            "name": "remove-background-clipboard",
            "title": "Remove Background from Clipboard",
            "subtitle": "RemBg",
            "description": "Remove background from an image in the clipboard",
            "mode": "no-view"
        }
    ],
    "preferences": [
        {
            "name": "outputSuffix",
            "title": "Output Suffix",
            "description": "Suffix added to the output filename (e.g. photo.png → photo_nobg.png)",
            "type": "textfield",
            "default": "_nobg",
            "required": false
        },
        {
            "name": "copyToClipboard",
            "title": "Copy Result to Clipboard",
            "description": "Automatically copy the result to the clipboard after processing",
            "type": "checkbox",
            "default": true,
            "label": "Copy to clipboard",
            "required": false
        },
        {
            "name": "processingMode",
            "title": "Processing Mode",
            "description": "Choose between best quality and faster processing",
            "type": "dropdown",
            "default": "quality",
            "required": true,
            "data": [
                {
                    "title": "Quality (best cutout)",
                    "value": "quality"
                },
                {
                    "title": "Speed (faster)",
                    "value": "speed"
                }
            ]
        }
    ],
    "dependencies": {
        "@raycast/api": "^1.104.0",
        "@raycast/utils": "^1.17.0"
    },
    "devDependencies": {
        "@raycast/eslint-config": "^1.0.11",
        "@types/node": "22.13.10",
        "@types/react": "19.0.10",
        "eslint": "^8.57.0",
        "prettier": "^3.4.2",
        "typescript": "^5.8.2"
    },
    "scripts": {
        "build": "ray build",
        "dev": "ray dev",
        "fix-lint": "ray lint --fix",
        "lint": "ray lint",
        "publish": "npx @raycast/api@latest publish"
    }
}