{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "display-scripts",
  "title": "Display Scripts",
  "description": "Toggle between side-by-side and stacked MacBook + external monitor layouts in one keystroke.",
  "icon": "extension-icon.png",
  "author": "Thomas",
  "license": "MIT",
  "platforms": ["macOS"],
  "categories": ["System", "Productivity"],
  "commands": [
    {
      "name": "toggle-arrangement",
      "title": "Toggle Arrangement",
      "subtitle": "Display Scripts",
      "description": "Switch between side-by-side and stacked MacBook + external monitor layouts.",
      "mode": "no-view"
    },
    {
      "name": "configure-displays",
      "title": "Configure Displays",
      "subtitle": "Display Scripts",
      "description": "Pick your displays from a list, identify them on-screen, and capture the two MacBook origin layouts.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "displayplacerPath",
      "type": "textfield",
      "title": "Path to displayplacer",
      "description": "Absolute path to the `displayplacer` binary. Install with `brew install displayplacer`.",
      "required": false,
      "default": "/opt/homebrew/bin/displayplacer",
      "placeholder": "/opt/homebrew/bin/displayplacer"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.83.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
