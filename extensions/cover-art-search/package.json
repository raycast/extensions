{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "cover-art-search",
  "title": "Cover Art Search",
  "description": "Search IMDb for movie, TV, anime, and game cover art, copy it, or send it straight to Capacities",
  "icon": "extension-icon.png",
  "author": "louay.md",
  "categories": [
    "Media",
    "Productivity"
  ],
  "keywords": [
    "imdb",
    "poster",
    "cover",
    "capacities",
    "movies",
    "image"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "search-cover-art",
      "title": "Search Cover Art",
      "description": "Search IMDb for movie, TV, anime, and game cover art",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@capacities/api": "^1.1.2",
    "@raycast/api": "^1.99.4",
    "@raycast/utils": "^1.17.0",
    "file-type": "^21.0.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "RAY_Target=x ray build",
    "dev": "RAY_Target=x ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  },
  "preferences": [
    {
      "name": "capacitiesApiToken",
      "type": "password",
      "required": false,
      "title": "Capacities API Token",
      "description": "Optional. Personal access token used to add cover images to your Capacities space. Generate one in the Capacities app under Settings → Capacities API (needs api:read and api:write scopes). Leave blank to use the extension for search and clipboard only."
    },
    {
      "name": "capacitiesObjectType",
      "type": "textfield",
      "required": false,
      "default": "Media",
      "title": "Object Type(s)",
      "description": "Choose all object types that should be matched across your Capacities space, separated by commas, exactly as they appear in the app (e.g. Movies, Games)."
    },
    {
      "name": "capacitiesCoverProperty",
      "type": "textfield",
      "required": false,
      "default": "Cover image",
      "title": "Cover Property",
      "description": "Name of the property on each object type that should point at the cover image. Default is always 'Cover image'."
    },
    {
      "name": "capacitiesImageCollection",
      "type": "textfield",
      "required": false,
      "title": "Image Collection",
      "description": "Optional. Name of an image collection to file the uploaded cover into (e.g. Cover Images)."
    }
  ],
  "contributors": [
    "talha_balaj"
  ]
}
