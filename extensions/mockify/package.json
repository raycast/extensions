{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "mockify",
  "title": "Mockify",
  "description": "Mock the input text to look like the Spongebob mock meme format.",
  "icon": "command-icon.png",
  "author": "annedouwe",
  "license": "MIT",
  "commands": [
    {
      "name": "mockify",
      "title": "Mockify",
      "description": "Get text from selection and replace it with the mockified version or mockify the clipboard contents and copy the result to the clipboard.",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.53.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "1.0.5",
    "@types/node": "18.8.3",
    "@types/react": "18.0.9",
    "eslint": "^7.32.0",
    "prettier": "^2.5.1",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
