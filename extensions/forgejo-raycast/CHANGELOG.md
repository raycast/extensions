# Forgejo Repositories Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Browse and search your Forgejo repositories with language and last-updated metadata
- Create new repositories from a form directly inside the list (⌘N) — private by default, with optional description
- Copy SSH clone URL with a configurable template (⌘C)
- Persistent cache for instant load on repeat opens — no empty flash
