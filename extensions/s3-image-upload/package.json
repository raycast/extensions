{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "s3-image-upload",
  "title": "S3 Image Upload",
  "description": "Upload images to AWS S3 and get the URL",
  "icon": "extension-icon.png",
  "author": "DangJin",
  "categories": [
    "Productivity",
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "upload-image",
      "title": "Upload Image",
      "description": "Select an image from Finder and upload to S3",
      "mode": "no-view"
    },
    {
      "name": "upload-clipboard",
      "title": "Upload from Clipboard",
      "description": "Upload image from clipboard to S3",
      "mode": "no-view"
    }
  ],
  "preferences": [
    {
      "name": "accessKeyId",
      "title": "AWS Access Key ID",
      "description": "Your AWS Access Key ID",
      "type": "password",
      "required": true
    },
    {
      "name": "secretAccessKey",
      "title": "AWS Secret Access Key",
      "description": "Your AWS Secret Access Key",
      "type": "password",
      "required": true
    },
    {
      "name": "bucket",
      "title": "S3 Bucket",
      "description": "The name of your S3 bucket",
      "type": "textfield",
      "required": true
    },
    {
      "name": "region",
      "title": "AWS Region",
      "description": "The AWS region of your S3 bucket (e.g., us-east-1)",
      "type": "textfield",
      "required": true,
      "default": "us-east-1"
    },
    {
      "name": "customDomain",
      "title": "Custom Domain",
      "description": "Optional custom domain for the S3 URL (e.g., https://cdn.example.com)",
      "type": "textfield",
      "required": false
    },
    {
      "name": "prefix",
      "title": "Path Prefix",
      "description": "Optional path prefix for uploaded files (e.g., images/)",
      "type": "textfield",
      "required": false,
      "default": ""
    }
  ],
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@raycast/api": "^1.87.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "22.10.2",
    "@types/react": "18.3.17",
    "eslint": "^8.57.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "ray build --skip-types -e dist -o dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
