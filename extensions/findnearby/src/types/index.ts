/** Travel modes for Google Maps. */
export enum TravelMode {
  Driving = "driving",
  Walking = "walking",
  Bicycling = "bicycling",
  Transit = "transit",
}

/** Place types for Google Maps. */
export enum PlaceType {
  Park = "park",
  Cafe = "cafe",
  JapaneseRestaurant = "japanese restaurant",
  ThaiRestaurant = "thai restaurant",
}

/** Origin options for location-based searches. */
export enum OriginOption {
  CurLoc = "curloc",
  Custom = "custom",
}
