{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "kesha-voice-kit",
  "title": "Kesha Voice Kit",
  "description": "Offline speech-to-text and text-to-speech, powered by the Kesha Voice Kit CLI. No API keys, no cloud — runs locally on Apple Silicon.",
  "icon": "extension-icon.png",
  "author": "drakulavich",
  "categories": ["Productivity", "Developer Tools"],
  "license": "MIT",
  "keywords": ["voice", "transcription", "stt", "tts", "kesha", "offline", "whisper", "parakeet"],
  "commands": [
    {
      "name": "transcribe-selected-file",
      "title": "Transcribe Selected Audio",
      "subtitle": "Kesha Voice Kit",
      "description": "Transcribe the audio file selected in Finder. Opens a detail view with the transcript + detected language; one keystroke copies it to the clipboard.",
      "mode": "view"
    },
    {
      "name": "speak-clipboard",
      "title": "Speak Clipboard",
      "subtitle": "Kesha Voice Kit",
      "description": "Synthesize speech from the current clipboard text and play it through the default output. English and Russian auto-routed; falls back to macOS system voices when AVSpeech is available.",
      "mode": "no-view"
    },
    {
      "name": "stop-speech",
      "title": "Stop Speech",
      "subtitle": "Kesha Voice Kit",
      "description": "Stop any in-flight playback from Speak Clipboard. Bind a global hotkey to interrupt speech mid-sentence.",
      "mode": "no-view"
    }
  ],
  "preferences": [
    {
      "name": "keshaBinPath",
      "title": "`kesha` binary path",
      "description": "Absolute path to the `kesha` CLI. Leave blank to auto-detect (~/.bun/bin, /opt/homebrew/bin, /usr/local/bin, ~/.npm-global/bin, ~/.local/bin). Raycast launches without your shell PATH, so a bare `kesha` lookup typically fails — set this if auto-detection misses your install.",
      "type": "textfield",
      "required": false,
      "default": "",
      "placeholder": "/opt/homebrew/bin/kesha"
    },
    {
      "name": "defaultVoice",
      "title": "Default voice (Speak Clipboard)",
      "description": "Voice id passed to `kesha say --voice`. Leave blank to auto-route by detected language: Kokoro `en-am_michael` (English) or Vosk `ru-vosk-m02` (Russian) when their TTS models are installed (`kesha install --tts`); otherwise the matching AVSpeech system voice (Samantha / Milena).",
      "type": "textfield",
      "required": false,
      "default": "",
      "placeholder": "en-am_michael, ru-vosk-m02, macos-com.apple.voice.compact.en-US.Samantha, ..."
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.85.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "^20.8.10",
    "@types/react": "19.0.10",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
