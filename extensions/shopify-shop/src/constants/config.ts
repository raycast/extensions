export const DEFAULT_STORE = "https://demostore.mock.shop";
export const CURRENCY_DEFAULT = "USD";
export const SEARCH_MIN_LENGTH = 2;
// Include the full set of searchable resource types by default so the
// app matches the command description and returns collections, pages,
// and articles as well as products.
export const DEFAULT_RESOURCE_TYPES = ["product", "page", "collection", "article"];
