# PostgreSQL Client Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Run Query, Browse Schema, Query History, and Manage Connections commands
- AI Extension with six tools: schema inspection, read queries, write queries behind a confirmation, `EXPLAIN`, `pg_stat_statements`, and connection listing
- Read-only statements are classified locally and then run inside a `READ ONLY` transaction
- macOS and Windows
