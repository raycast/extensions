# LightRAG Knowledge Base Changelog

## [Initial release] - {PR_MERGE_DATE}

- Query LightRAG with natural language and structured retrieval (RAG modes, entities, chunks, references)
- List documents and document status counts; server health, pipeline status, and track status
- Insert text notes; knowledge graph tools (labels, search, popular labels, subgraph, entity check)
- Raycast AI tools with `ai.yaml` instructions; subgraph responses sized for Raycast limits
