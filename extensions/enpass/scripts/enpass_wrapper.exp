#!/usr/bin/expect -f

set timeout 30
set password $env(ENPASS_MASTER_PASSWORD)
spawn {*}$argv

expect {
    -re "(?i)password" {
        send "$password\r"
        expect eof
    }
    eof
}

catch wait result
exit [lindex $result 3]
