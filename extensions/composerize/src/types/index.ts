declare module "composerize" {
  const composerize: (arg: string) => string;
  export default composerize;
}

declare module "decomposerize" {
  const decomposerize: (arg: string) => string;
  export default decomposerize;
}
