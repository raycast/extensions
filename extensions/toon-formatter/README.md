# TOON Formatter

Formats a JSON file into the TOON structure — a compact, LLM-friendly format that saves tokens, improves parsing consistency, and reduces hallucination surface. If the JSON is stringified, it will be parsed automatically before converting to TOON.