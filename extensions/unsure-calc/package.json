{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "unsure-calc",
  "title": "Unsure Calc",
  "description": "Lightweight probabilistic calculator that lets you type ranges (with `~`) and arithmetic to see exact bounds plus simulated outcomes and a text histogram.",
  "icon": "extension-icon.png",
  "author": "PythonicNinja",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "calc",
      "title": "Calc",
      "subtitle": "ranges (with `~`) and arithmetic calculation",
      "description": "Lightweight probabilistic calculator",
      "mode": "view",
      "preferences": [
        {
          "name": "samples",
          "type": "textfield",
          "required": false,
          "title": "Number of Samples",
          "description": "Number of Monte Carlo samples for simulation",
          "default": "10000"
        },
        {
          "name": "histogramBins",
          "type": "textfield",
          "required": false,
          "title": "Histogram Bins",
          "description": "Number of bins in the histogram",
          "default": "20"
        },
        {
          "name": "histogramWidth",
          "type": "textfield",
          "required": false,
          "title": "Histogram Width",
          "description": "Maximum width of histogram bars",
          "default": "40"
        },
        {
          "name": "barChar",
          "type": "textfield",
          "required": false,
          "title": "Bar Character",
          "description": "Character used for histogram bars",
          "default": "█"
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.1",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
