# YouTube Downloader

Download YouTube video from Raycast. To be able to use this extension, you need to have `ffmpeg` installed on your machine.

To install `ffmpeg`, first install [Homebrew](https://brew.sh/), then run the following command in your terminal:

```bash
brew install ffmpeg
```
