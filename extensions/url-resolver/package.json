{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "url-resolver",
  "title": "URL Resolver",
  "description": "Resolve URLs using DNS-over-HTTPS. Get the final destination from shortened or redirect links.",
  "version": "2.1.0",
  "icon": "extension-icon.png",
  "author": "kevintraver",
  "categories": [
    "Developer Tools"
  ],
  "keywords": [
    "dns",
    "doh",
    "redirect",
    "url",
    "resolve",
    "shortener",
    "unshorten",
    "trace"
  ],
  "license": "MIT",
  "type": "module",
  "commands": [
    {
      "name": "resolve-url",
      "title": "Resolve URL",
      "description": "Resolve a URL to its final destination using DNS-over-HTTPS",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "dnsProvider",
      "type": "dropdown",
      "required": true,
      "title": "Default Resolver",
      "description": "DNS-over-HTTPS provider to use for resolution",
      "data": [
        {
          "title": "Cloudflare (1.1.1.1)",
          "value": "cloudflare"
        },
        {
          "title": "Google (8.8.8.8)",
          "value": "google"
        },
        {
          "title": "Quad9 (9.9.9.9)",
          "value": "quad9"
        },
        {
          "title": "OpenDNS (208.67.222.222)",
          "value": "opendns"
        }
      ]
    },
    {
      "name": "maxRedirects",
      "type": "textfield",
      "required": false,
      "title": "Max Redirects",
      "description": "Maximum number of redirects to follow",
      "default": "10"
    },
    {
      "name": "timeout",
      "type": "textfield",
      "required": false,
      "title": "Timeout (Ms)",
      "description": "Request timeout in milliseconds",
      "default": "3000"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.100.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
