{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "product-sharing",
  "title": "Product Sharing (private)",
  "description": "Use natural language to find the perfect product for your needs.",
  "icon": "extension-icon.png",
  "author": "evanlong",
  "categories": [
    "Communication"
  ],
  "preferences": [
    {
      "name": "apiKey",
      "type": "password",
      "required": true,
      "title": "API Key",
      "description": "Your API key for the service"
    }
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "list",
      "title": "Products List (private)",
      "description": "List all submitted products",
      "mode": "view"
    },
    {
      "name": "submit",
      "title": "Product Submit (private)",
      "description": "Submit a new product",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.101.1",
    "@raycast/utils": "^2.2.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "24.1.0",
    "@types/react": "19.1.8",
    "eslint": "^9.32.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
