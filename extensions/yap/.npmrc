# Use npm as the package manager for this project
# This overrides any workspace-level pnpm configuration
package-manager=npm

# Ensure package-lock.json is generated and used
package-lock=true

# Strict peer dependencies for Raycast extensions
legacy-peer-deps=false
