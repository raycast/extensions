{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "simplesnippet",
  "title": "Simplesnippet",
  "description": "Create and manage multiple plain text prompts, messages, snippets easily and quickly.",
  "icon": "simple-snippet-icon.png",
  "author": "adroa",
  "license": "MIT",
  "categories": [
    "Productivity",
    "Other"
  ],
  "commands": [
    {
      "name": "simplesnippet",
      "title": "Simplesnippet",
      "description": "Create and manage multiple plain text snippets, prompts, messages .etc",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.2",
    "@raycast/utils": "^2.2.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^24.5.2",
    "@types/react": "^19.1.14",
    "eslint": "^9.36.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}