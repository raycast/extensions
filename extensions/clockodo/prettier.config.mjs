// TODO: Ideally we'd use the shared config from @peerigon/configs,
// but this causes issues in the Raycast CI pipeline (think because of old Node.js version)
// import { config } from "@peerigon/configs/prettier";

export default {
//   ...config,
  printWidth: 120,
  singleQuote: false,
};
