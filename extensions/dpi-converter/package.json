{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "dpi-converter",
  "title": "DPI Converter",
  "description": "Convert pixels to centimeters, millimeters, inches, and vice-versa! Useful for printers.",
  "icon": "dpi-converter-icon.png",
  "author": "Qualigraphe",
  "owner": "Qualigraphe",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Design Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "convert-to-px-cm-mm-in",
      "title": "Convert to px, cm, mm, in",
      "description": "Convert pixels to centimeters, millimeters, inches, and vice-versa.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"It seems like you are trying to publish the Raycast extension to npm. If you did intend to publish it to npm, remove the 'prepublishOnly' script and rerun 'npm publish' again. If you wanted to publish it to the Raycast Store instead, use 'npm run publish' instead.\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
