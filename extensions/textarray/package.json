{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "textarray",
  "title": "TextArray",
  "description": "540+ text tools that run locally — transform the selected text or clipboard: case, encoding, cleaning, formatting, ciphers and more. Nothing leaves your machine.",
  "icon": "icon.png",
  "author": "apptiary_com",
  "categories": [
    "Productivity",
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "run-tool",
      "title": "Run TextArray Tool",
      "subtitle": "TextArray",
      "description": "Search a text tool and run it on the selected text or clipboard, then paste or copy the result.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^2.0.5"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "eslint": "^10.9.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "publish": "npx @raycast/api@latest publish"
  },
  "platforms": [
    "macOS",
    "Windows"
  ]
}
