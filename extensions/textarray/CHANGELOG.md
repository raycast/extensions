# TextArray Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Run TextArray Tool command: search 150+ text tools and run them on the selected text or clipboard.
- Paste (`↵`), copy (`⌘C`), open on textarray.com (`⌘O`), and reload input (`⌘R`).
- Everything runs locally — no network calls.
