// AUTO-GENERATED by scripts/raycast/generate.mjs — do not edit.
// Types for the self-contained catalog.gen.js bundle (built from src/data/tools.ts).

export interface TallyPart {
  text: string;
  accent?: boolean;
}

export interface ToolResult {
  output: string;
  tally: TallyPart[];
  error?: string;
  diff?: { type: 'same' | 'add' | 'del'; text: string }[];
  qrSvg?: string;
}

export type ToolRun = (
  input: string,
  options: Record<string, string | number | boolean>,
  ctx: { second?: string; locale: string },
) => ToolResult | Promise<ToolResult>;

export interface CatalogTool {
  slug: string;
  name: string;
  desc: string;
  category: string;
  mode: 'transform' | 'generate';
  run: ToolRun;
}

export declare const catalog: CatalogTool[];
