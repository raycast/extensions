# Assets

Drop the following files here before running `bun run dev`:

- `extension-icon.png` — 512×512 PNG, the extension's primary icon

Command-specific icons can also live here and be referenced from `package.json`.
