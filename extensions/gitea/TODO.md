# necessary

- match categories for `issue-mine` and `issue-search` ([`open`, `closed`, `all`] or [`mentions me`, `assigned to me`, etc.]). category selection depends on the available options for the query search syntax
- improve query search syntax for issue search
  - if owner + repo is specified, use other api endpoint

# optional

- add action to repository to search issues for given repository (reuse `issue-search` with the use of query search syntax)
