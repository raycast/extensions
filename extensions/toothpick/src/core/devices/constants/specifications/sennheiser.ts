import { DeviceDefinition } from "../../devices.types";

const devices: Record<string, DeviceDefinition> = {
  "0x004B": {
    name: "Momentum M2",
    main: "icons/devices/sennheiser/momentum.m2.svg",
  },
};

export default devices;
