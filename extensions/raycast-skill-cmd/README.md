# Paste Agent Skill

A local Raycast extension that lists `~/.agents/skills/**/SKILL.md` and pastes the selected skill in Pi's `<skill>` format into the foreground app.

```bash
bun install
bun run dev
```
