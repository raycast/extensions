{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "vps-upload",
  "title": "VPS Upload",
  "description": "Upload files to your server over SSH with a live progress bar, and copy the remote path to your clipboard — ideal for referencing local files inside a remote SSH session.",
  "icon": "icon.png",
  "author": "henrikkvamme",
  "categories": ["Developer Tools", "Productivity"],
  "platforms": ["macOS"],
  "license": "MIT",
  "commands": [
    {
      "name": "upload-selection",
      "title": "Upload Finder Selection",
      "subtitle": "VPS Upload",
      "description": "Upload the file(s) selected in Finder to your server and copy the remote path. No window — give it a hotkey.",
      "mode": "no-view"
    },
    {
      "name": "upload",
      "title": "Upload Files",
      "subtitle": "VPS Upload",
      "description": "Pick or drop files to upload to your server, with a live progress bar, and copy the remote path.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.80.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.10",
    "react": "^19.0.0",
    "typescript": "^5.5.0",
    "@raycast/eslint-config": "^1.0.11",
    "eslint": "^8.57.0",
    "prettier": "^3.3.0"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "publish": "ray publish"
  }
}
