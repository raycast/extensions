{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "heidisql-sessions",
  "title": "HeidiSQL Sessions",
  "description": "Search and launch your HeidiSQL sessions from Raycast.",
  "icon": "extension-icon.png",
  "author": "THEN00P",
  "license": "MIT",
  "platforms": [
    "Windows",
    "macOS"
  ],
  "categories": [
    "Developer Tools",
    "Productivity"
  ],
  "commands": [
    {
      "name": "search-sessions",
      "title": "Search Sessions",
      "subtitle": "HeidiSQL",
      "description": "Search and launch your HeidiSQL sessions.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "heidiExePath",
      "title": "HeidiSQL Executable",
      "description": "Optional. Path to heidisql.exe (Windows) or heidisql.app (macOS). Leave empty to auto-detect: on Windows from PATH, Scoop, or the default install location; on macOS from /Applications or ~/Applications.",
      "type": "file",
      "required": false
    },
    {
      "name": "portableMode",
      "title": "Session Source",
      "label": "Force portable mode",
      "description": "Windows only. Portable mode is auto-detected when a portable_settings.txt sits next to the executable (e.g. Scoop installs). Enable this only to force reading from portable_settings.txt when auto-detection can't. Has no effect on macOS, which always reads ~/.config/heidisql/settings.json.",
      "type": "checkbox",
      "default": false,
      "required": false
    },
    {
      "name": "showOpenEntry",
      "title": "Extra Entry",
      "label": "Show an \"Open HeidiSQL\" entry",
      "description": "Always show an entry that opens HeidiSQL without a session.",
      "type": "checkbox",
      "default": true,
      "required": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.23",
    "@raycast/utils": "^2.2.7"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.2.0",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.17",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
