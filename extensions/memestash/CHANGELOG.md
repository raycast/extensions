# MemeStash Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Search your image/meme library in a thumbnail grid, fuzzy-filtered by name and keywords
- Paste the selected image into the frontmost app, with copy and show-in-Finder fallbacks
- Add images from a file picker, the clipboard, or the current Finder selection
- Edit names and keywords, and delete entries (moved to the Trash)
- Configurable library folder; rebuild the index to import images added outside the extension
