## [Initial Release] - {PR_MERGE_DATE}

- Mask authentication tokens and credentials from curl commands
- Supports Authorization headers (Bearer, Basic, Digest, etc.)
- Supports API keys (X-API-Key, X-Auth-Token, etc.)
- Supports user credentials (-u flag)
- Supports URL embedded credentials
- Supports query parameters (api_key, access_token, etc.)
- Supports JSON body sensitive fields
- Supports Cookie headers
- Supports both -H and --header syntax
- Instant clipboard processing with toast notifications