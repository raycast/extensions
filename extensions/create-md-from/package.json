{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "create-md-from",
  "title": "Create Markdown from",
  "description": "Create Markdown (.md) files from selected text, the clipboard, or the current browser tab.",
  "icon": "extension-icon.png",
  "author": "juan_diego_vazquez",
  "categories": ["Productivity"],
  "license": "MIT",
  "preferences": [
    {
      "name": "defaultFolder",
      "title": "Default Folder",
      "description": "Where new .md files are saved by default. Defaults to ~/Desktop if empty.",
      "type": "directory",
      "required": false
    }
  ],
  "commands": [
    {
      "name": "from-selection",
      "title": "From Selection",
      "subtitle": "Create Markdown",
      "description": "Create a Markdown file from the currently selected text.",
      "mode": "view"
    },
    {
      "name": "from-clipboard",
      "title": "From Clipboard",
      "subtitle": "Create Markdown",
      "description": "Create a Markdown file from the clipboard. If it holds a URL, the article is fetched and converted.",
      "mode": "view"
    },
    {
      "name": "from-current-tab",
      "title": "From Current Tab",
      "subtitle": "Create Markdown",
      "description": "Create a Markdown file from the current browser tab (requires the Raycast Browser Extension).",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "@raycast/api": "^1.99.0",
    "@raycast/utils": "^1.19.0",
    "linkedom": "^0.18.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.1.1",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "@types/turndown": "^5.0.5",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "publish": "npx @raycast/api@latest publish"
  }
}
