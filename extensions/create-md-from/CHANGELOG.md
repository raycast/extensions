# Create Markdown From Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Create Markdown files from selected text, the clipboard, or the current browser tab
- Smart clipboard handling: URLs are fetched and converted to Markdown, and rich (HTML) copies keep their images and formatting
- Configurable default folder that remembers the last folder you used
- Safe saving with Finder-style automatic name de-duplication
