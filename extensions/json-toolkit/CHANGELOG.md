## [Initial Version] - {PR_MERGE_DATE}

- Added five Raycast-native commands for formatting JSON, formatting selected text, formatting clipboard text, escaping JSON strings, and unescaping one JSON string layer.
- Added strict JSON formatting with conservative best-effort formatting for invalid or relaxed JSON-like text.
- Added copy actions for pretty, minified, fallback, escaped, and unescaped results.
