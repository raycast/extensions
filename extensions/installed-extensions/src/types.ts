export type dataType = {
  [key: string]: any;
  path: string;
  name: string;
  icon: string;
  title: string;
  author: string;
  commands: string;
  isLocalExtension: boolean;
  link: string;
};

export type optionType = { id: string; name: string };
