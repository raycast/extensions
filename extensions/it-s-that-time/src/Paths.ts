import { environment } from "@raycast/api";

const TIMER_PATH = `${environment.supportPath}/timers`;
const MESSAGE_SCRIPT_FILE = `${environment.assetsPath}/scripts/large-message.py`;

export default {
  TIMER_PATH,
  MESSAGE_SCRIPT_FILE,
};
