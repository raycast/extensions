export interface Preferences {
  modelsResultsCount: string;
  historyCount: string;
  showLinkToSearchResultsInListView: boolean;
}

export enum EntityType {
  Model = "model",
  Dataset = "dataset",
}
