# Menu Command Bar Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Search and invoke any menu item in the frontmost app
- Per-app memory of recently used items, pinned to the top
- Keyboard shortcuts shown inline as accessories
- Reliable invocation via the macOS Accessibility API (works in Adobe apps where AppleScript-based pickers fail)
