import { CommandRoot } from "./components/CommandRoot";

/** "Search Word" command — opens the shared search list with an empty query. */
export default function AddCard() {
  return <CommandRoot />;
}
