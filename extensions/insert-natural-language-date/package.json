{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "insert-natural-language-date",
  "title": "Insert Natural Language Date",
  "description": "Convert a natural language prompt into a date and inserts it.",
  "icon": "extension-icon.png",
  "author": "jose-elias-alvarez",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "format",
      "title": "Default Date Format",
      "type": "textfield",
      "default": "YYYY-MM-DD",
      "description": "Default date format string to use when unspecified.",
      "required": true
    }
  ],
  "commands": [
    {
      "name": "insert-natural-language-date",
      "title": "Insert Natural Language Date",
      "description": "Converts a natural language prompt into a formatted date and inserts it into the frontmost application",
      "mode": "no-view",
      "arguments": [
        {
          "name": "prompt",
          "placeholder": "Date Prompt",
          "type": "text",
          "required": false
        },
        {
          "name": "format",
          "placeholder": "Date Format",
          "type": "text",
          "required": false
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.84.10",
    "@raycast/utils": "^1.17.0",
    "chrono-node": "^2.7.7"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "20.8.10",
    "@types/react": "18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
