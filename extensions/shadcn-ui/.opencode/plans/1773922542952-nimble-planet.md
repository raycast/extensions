Investigate how the official `shadcn/ui` repo structure changed, compare that against this Raycast extension's assumptions, and implement the smallest safe fixes, prioritizing the broken `search-components` command.

Recommended approach

1. Fix `search-components` first

- Update `/Users/carles/src/shadcn-ui/src/search-components.tsx` so it no longer assumes flat component docs directly under `apps/v4/content/docs/components`.
- Per the latest direction, keep the user-facing command focused on the `radix` collection only for now.
- Fetch component files from `/apps/v4/content/docs/components/radix` and treat that as the canonical component source.
- Do not merge in `base` results yet; this keeps the command simple and aligns with directing users to the preferred variant.

2. Fix component detail and browser URLs

- The extension currently expects:
  - raw detail files at `.../apps/v4/content/docs/components/<slug>.mdx`
  - public docs routes at `/docs/components/<slug>`
- Upstream now uses sectioned paths, so update the command to build:
  - raw/detail URLs as `.../apps/v4/content/docs/components/radix/${slug}.mdx`
  - browser URLs as `https://ui.shadcn.com/docs/components/radix/${slug}`
- Keep the rest of the UI and parsing logic unchanged unless the new paths require a tiny follow-up.

3. Prefer the smallest targeted fix

- Do patch only `radix`; that is now the preferred user-facing behavior and the smallest safe repair.
- Do not redesign the command, query model, or list UI.
- If convenient during implementation, prefer GitHub API `download_url` values over manually concatenating raw GitHub paths, but treat that as optional cleanup rather than a required refactor.

4. Preserve a clear explanation of what changed upstream

- Old extension assumption:
  - top-level `.mdx` files under `apps/v4/content/docs/components`
  - no section segment in raw URLs or site URLs
- Current upstream structure:
  - actual docs live in `apps/v4/content/docs/components/radix/*.mdx`
  - and `apps/v4/content/docs/components/base/*.mdx`
  - the top-level directory now contains directories and metadata files like `index.mdx` and `meta.json`
  - canonical docs routes are namespaced by section, e.g. `/docs/components/radix/accordion`

5. Keep `base` as a deliberate non-goal for now

- Document in the implementation notes that `base` exists upstream but is intentionally excluded from search results for now.
- This avoids duplicate or near-duplicate search results while keeping the command behavior straightforward for end users.
- If the project later wants both collections, add a follow-up enhancement with explicit labeling or filtering instead of mixing them silently.

6. Treat registry fixes as optional follow-up work

- `/Users/carles/src/shadcn-ui/src/search-registries.tsx` still assumes `https://ui.shadcn.com/r/registries.json` returns an object map.
- Current upstream payload is an array.
- Minimal correctness fix if included:
  - parse as `RegistryAPIResponse[]`
  - replace `Object.entries(data).map(...)` with `data.map(...)`
- Keep this separate from the main `search-components` repair unless there is room for one extra low-risk cleanup.

7. Note other stale assumptions without expanding scope too far

- `/Users/carles/src/shadcn-ui/src/documentation/shadcn-ui-documentation.ts` is manually maintained and its header comment points to an outdated upstream location.
- `/Users/carles/src/shadcn-ui/src/search-examples.tsx` appears mostly fine, though filtering GitHub contents by `type === "dir"` would be more robust than excluding files by extension.
- Mention these as follow-ups, not blockers for the main repair.

Critical files to modify

- `/Users/carles/src/shadcn-ui/src/search-components.tsx`

Optional follow-up files

- `/Users/carles/src/shadcn-ui/src/search-registries.tsx`
- `/Users/carles/src/shadcn-ui/src/constants.ts`
- `/Users/carles/src/shadcn-ui/src/documentation/shadcn-ui-documentation.ts`

Verification after implementation

- Run the Raycast command behind `search-components` and confirm it lists `radix` components again.
- Open at least one result and confirm the detail view loads instead of failing.
- Trigger the browser action and confirm the generated URL resolves under `/docs/components/radix/...`.
- If the registries follow-up is included, verify parsing still succeeds against the live `registries.json` payload shape.

Expected patch scope

- Primary minimal fix: one focused patch in `/Users/carles/src/shadcn-ui/src/search-components.tsx`
- Optional secondary cleanup: one small patch in `/Users/carles/src/shadcn-ui/src/search-registries.tsx`
