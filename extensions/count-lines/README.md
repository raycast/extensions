# Count Lines – Raycast Extension

Shows the number of **lines** in your current selection (falls back to clipboard if selection is unavailable).

## Local install
```bash
npm install
npx ray develop
```

## Publish
```bash
npm run publish
```
