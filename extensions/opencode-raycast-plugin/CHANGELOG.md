# Opencode Info Changelog

## [Opencode Info] - {PR_MERGE_DATE}

- Surfaced OpenCode Go usage in the menu bar and a full view: the three limit windows (rolling 5h, weekly, monthly) with used-percent and reset time.
- Added a live model catalog of OpenCode Go and OpenCode Zen models with current $/M pricing, sortable by cost, quota, or name.
- Added daily picks (stretch quota and best value) computed from current pricing.
- Added per-model quota estimates for the rolling 5h window.
- Added distinct error states for no key, bad key, missing entitlement, and offline (offline keeps the last-known data).