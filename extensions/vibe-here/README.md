# Vibe Here

Opens Warp or Ghostty in the selected folder and starts `vibe`.

Works like **Open in Warp**:

1. Select a folder (or file) in Finder
2. Run **Vibe Here**
3. Terminal opens there and runs `vibe`

If nothing is selected, File Search / the command argument is used, then the front Finder window.
