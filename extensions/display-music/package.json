{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "display-music",
  "title": "Display Music",
  "description": "Shows the current Apple Music album artwork and track info in your menu bar",
  "icon": "extension-icon.png",
  "author": "GuyMagen",
  "categories": ["Media"],
  "license": "MIT",
  "commands": [
    {
      "name": "now-playing",
      "title": "Now Playing",
      "subtitle": "Display Music",
      "description": "Shows album artwork and current track info in the menu bar",
      "mode": "menu-bar",
      "interval": "10s"
    }
  ],
  "preferences": [
    {
      "name": "showTrackTitle",
      "title": "Show Track Title",
      "description": "Display the track name next to the artwork in the menu bar",
      "type": "checkbox",
      "default": true,
      "required": false,
      "label": "Show track title in menu bar"
    },
    {
      "name": "showArtistName",
      "title": "Show Artist Name",
      "description": "Display the artist name next to the artwork in the menu bar",
      "type": "checkbox",
      "default": false,
      "required": false,
      "label": "Show artist name in menu bar"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.83.1",
    "run-applescript": "^7.0.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "22.5.2",
    "@types/react": "18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build --skip-types -e dist -o dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"Error: no publish\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
