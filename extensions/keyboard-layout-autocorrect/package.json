{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "keyboard-layout-autocorrect",
  "title": "Keyboard Layout Autocorrect",
  "description": "Automatically corrects text typed with the wrong keyboard layout and improves search accuracy by also using fuzzy search, saving time and enhancing your typing experience.",
  "icon": "extension-icon.png",
  "author": "ruslan_borukha",
  "license": "MIT",
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  },
  "commands": [
    {
      "name": "search",
      "title": "Smart Search Applications",
      "description": "Search with keyboard layout autocorrect and fuzzy matching",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.99.0",
    "@raycast/utils": "^1.17.0",
    "fuse.js": "^7.1.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
