# ghq plus Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Add List Repositories to search every repository `ghq list` reports and open it in an editor or terminal
- Add Get Repository to clone with `ghq get` over HTTPS or SSH
- Run the user's ghq binary so multiple roots and Git `ghq.root` settings are respected
- Prefill GitHub URLs from the clipboard and cancel an in-progress clone from the toast
