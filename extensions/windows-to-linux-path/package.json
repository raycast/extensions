{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "windows-to-linux-path",
  "title": "Windows to Linux Path",
  "description": "Converts a Windows path into a Linux one and copy it to the clipboard.",
  "icon": "extension-icon.png",
  "license": "MIT",
  "commands": [
    {
      "name": "windows-linux-path",
      "title": "Convert Windows to Linux Path",
      "description": "Converts a Windows path into a Linux one.",
      "mode": "no-view",
      "arguments": [
        {
          "name": "path",
          "type": "text",
          "required": true,
          "placeholder": "C:\\..."
        }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.103.0",
    "@raycast/utils": "^2.2.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "build": "ray build",
    "publish": "npx @raycast/api@latest publish",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1"
  },
  "author": "Showtc",
  "platforms": [
    "Windows",
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ]
}
