# FNM Raycast 扩展 - 项目总结

## 项目概述

这是一个为 Raycast 开发的 fnm (Fast Node Manager) 扩展,允许用户通过 Raycast 快速管理 Node.js 版本。

## 技术栈

- **开发语言**: TypeScript
- **UI 框架**: React
- **API**: Raycast API v1.80.0
- **包管理**: npm
- **代码规范**: ESLint + Prettier

## 项目结构

```
fnm-raycast/
├── src/                          # 源代码目录
│   ├── utils/
│   │   └── fnm.ts               # fnm 命令行工具封装
│   ├── list-versions.tsx         # 列出已安装版本命令
│   ├── install-version.tsx       # 安装新版本命令
│   ├── use-version.tsx           # 切换版本命令
│   └── uninstall-version.tsx     # 卸载版本命令
│
├── assets/                       # 资源文件目录
│   ├── icon.png                 # 扩展图标 (需要创建)
│   ├── icon-template.svg        # SVG 图标模板
│   └── README.md                # 图标说明文档
│
├── scripts/                      # 脚本目录
│   ├── generate-placeholder-icon.js  # 图标生成脚本
│   └── create-icon.md           # 图标创建说明
│
├── .vscode/                      # VS Code 配置
│   ├── settings.json            # 编辑器设置
│   └── extensions.json          # 推荐扩展
│
├── package.json                  # 项目配置和依赖
├── tsconfig.json                # TypeScript 配置
├── eslint.config.mjs            # ESLint 配置
├── .prettierrc                  # Prettier 配置
├── .gitignore                   # Git 忽略文件
├── .cursorignore                # Cursor 忽略文件
├── .npmrc                       # npm 配置
│
├── README.md                     # 项目说明文档
├── INSTALL.md                   # 安装指南
├── QUICKSTART.md                # 快速开始指南
├── CONTRIBUTING.md              # 贡献指南
├── CHANGELOG.md                 # 更新日志
└── PROJECT_SUMMARY.md           # 项目总结 (本文件)
```

## 核心功能

### 1. 列出已安装版本 (`list-versions.tsx`)
- 显示所有已安装的 Node.js 版本
- 标记当前使用的版本(绿色)
- 标记默认版本(蓝色)
- 支持快速切换和设置默认版本
- 快捷键: ⌘+R 刷新, ⌘+D 设置默认

### 2. 安装新版本 (`install-version.tsx`)
- 快速安装 LTS 版本
- 快速安装最新版本
- 自定义版本号安装
- 浏览可用版本列表(最新 50 个)
- 支持命令行参数传入版本号

### 3. 切换版本 (`use-version.tsx`)
- 从已安装版本中选择
- 实时显示当前使用的版本
- 自动刷新列表

### 4. 卸载版本 (`uninstall-version.tsx`)
- 安全卸载机制
- 防止卸载当前使用的版本
- 确认对话框防止误操作
- 显示版本状态标签

## 核心工具函数 (`src/utils/fnm.ts`)

| 函数 | 功能 | 返回值 |
|------|------|--------|
| `checkFnmInstalled()` | 检查 fnm 是否安装 | `Promise<boolean>` |
| `getInstalledVersions()` | 获取已安装版本列表 | `Promise<NodeVersion[]>` |
| `getCurrentVersion()` | 获取当前使用版本 | `Promise<string \| null>` |
| `installVersion(version)` | 安装指定版本 | `Promise<{success, message}>` |
| `useVersion(version)` | 切换到指定版本 | `Promise<{success, message}>` |
| `setDefaultVersion(version)` | 设置默认版本 | `Promise<{success, message}>` |
| `uninstallVersion(version)` | 卸载指定版本 | `Promise<{success, message}>` |
| `getRemoteVersions()` | 获取可用远程版本 | `Promise<string[]>` |

## 用户体验特性

### 友好的错误处理
- fnm 未安装时显示友好提示
- 操作失败时显示详细错误信息
- 防止误操作(如卸载当前版本)

### 实时反馈
- 操作过程中显示加载动画
- 操作完成后显示成功/失败提示
- 自动刷新列表显示最新状态

### 视觉设计
- 使用颜色标记不同状态(绿色=当前, 蓝色=默认)
- 使用图标增强可读性
- 使用标签(tags)显示版本状态

### 快捷键支持
- ⌘+R: 刷新列表
- ⌘+D: 设置为默认版本

## 开发命令

```bash
# 安装依赖
npm install

# 开发模式(在 Raycast 中加载)
npm run dev

# 构建扩展
npm run build

# 代码检查
npm run lint

# 自动修复代码问题
npm run fix-lint

# 发布到 Raycast Store
npm run publish
```

## 依赖项

### 运行时依赖
- `@raycast/api`: ^1.80.0 - Raycast 核心 API
- `@raycast/utils`: ^1.16.0 - Raycast 工具函数

### 开发依赖
- `@raycast/eslint-config`: ^1.0.11 - ESLint 配置
- `@types/node`: 20.8.10 - Node.js 类型定义
- `@types/react`: 18.3.3 - React 类型定义
- `eslint`: ^8.57.0 - 代码检查工具
- `prettier`: ^3.2.5 - 代码格式化工具
- `typescript`: ^5.4.5 - TypeScript 编译器

## 待完成事项

### 必需
- [ ] 创建 `assets/icon.png` 文件(512x512 像素)

### 可选增强
- [ ] 添加版本搜索功能
- [ ] 添加版本比较功能
- [ ] 支持 .nvmrc 文件检测
- [ ] 添加版本别名管理
- [ ] 显示 Node.js 版本发布日期
- [ ] 添加版本更新提醒
- [ ] 支持批量操作

## 测试清单

在发布前,请确保测试以下功能:

- [ ] fnm 未安装时显示正确提示
- [ ] 列出已安装版本正常工作
- [ ] 安装 LTS 版本正常工作
- [ ] 安装最新版本正常工作
- [ ] 安装自定义版本正常工作
- [ ] 切换版本正常工作
- [ ] 设置默认版本正常工作
- [ ] 卸载版本正常工作
- [ ] 防止卸载当前版本
- [ ] 所有快捷键正常工作
- [ ] 错误处理正确显示
- [ ] 加载状态正常显示

## 发布准备

1. **创建图标**: 确保 `assets/icon.png` 存在
2. **测试功能**: 运行所有测试清单项目
3. **检查代码**: 运行 `npm run lint`
4. **更新文档**: 确保所有文档是最新的
5. **构建扩展**: 运行 `npm run build`
6. **发布**: 运行 `npm run publish`

## 许可证

MIT License

## 作者

gefangshuai

## 相关链接

- [fnm GitHub](https://github.com/Schniz/fnm)
- [Raycast 开发文档](https://developers.raycast.com)
- [Raycast Store](https://www.raycast.com/store)

## 版本历史

### v1.0.0 (2026-01-08)
- 🎉 初始版本发布
- ✨ 实现核心功能:列出、安装、切换、卸载 Node.js 版本
- 📝 完整的中文文档
- 🎨 友好的用户界面

---

**最后更新**: 2026-01-08
**状态**: 开发完成,待创建图标后即可使用
