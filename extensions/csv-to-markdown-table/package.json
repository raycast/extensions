{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "csv-to-markdown-table",
  "title": "CSV to Markdown Table",
  "description": "Convert CSV/TSV data from your clipboard to Markdown table format",
  "icon": "extension-icon.png",
  "author": "k-narusawa",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Documentation",
    "Developer Tools",
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "csv-to-markdown",
      "title": "Convert CSV to Markdown",
      "description": "Instantly convert CSV/TSV in your clipboard to a Markdown table",
      "mode": "no-view"
    },
    {
      "name": "markdown-to-csv",
      "title": "Convert Markdown to CSV",
      "description": "Convert Markdown table in your clipboard to CSV format",
      "mode": "no-view"
    },
    {
      "name": "markdown-to-tsv",
      "title": "Convert Markdown to TSV",
      "description": "Convert Markdown table in your clipboard to TSV format",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.1",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^4.0.16"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
