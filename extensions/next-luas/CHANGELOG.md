# Next Luas Changelog

## [Initial Version] - {PR_MERGE_DATE}

- Find your closest Luas stop via GPS (CoreLocationCLI) with IP and manual-coord fallbacks.
- Real-time inbound and outbound forecasts from TII's public feed.
- Auto-refresh every 20 seconds; re-rank closest stop every 60 seconds.
- Cycle through the three nearest stops with `⌘S`.
- Copy tram info to clipboard with `⌘⇧C`.
- Bundled snapshot of all 67 Luas stops so distances work offline.
- Service-disruption messages surfaced at the top of the list.
