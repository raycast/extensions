{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "spellbook",
  "title": "Spellbook",
  "description": "Your parameterized shell command library. Save commands with {{param=default}} placeholders, run them with defaults in two keystrokes, or override any parameter at runtime.",
  "icon": "icon.png",
  "author": "netbull",
  "categories": ["Developer Tools", "Productivity"],
  "license": "MIT",
  "commands": [
    {
      "name": "search-commands",
      "title": "Search Commands",
      "subtitle": "Spellbook",
      "description": "Fuzzy-find your saved commands; Enter runs with defaults, Cmd+Enter overrides parameters first.",
      "mode": "view"
    },
    {
      "name": "save-command",
      "title": "Save Command",
      "subtitle": "Spellbook",
      "description": "Save a new parameterized command; the template is prefilled from your clipboard when it looks like shell.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "libraryPath",
      "type": "textfield",
      "required": false,
      "title": "Library File",
      "description": "Path to the JSON file holding your commands (supports ~).",
      "default": "~/.config/spellbook/commands.json",
      "placeholder": "~/.config/spellbook/commands.json"
    },
    {
      "name": "terminalApp",
      "type": "dropdown",
      "required": false,
      "title": "Terminal App",
      "description": "Terminal used when a command's run mode is Terminal.",
      "default": "Terminal",
      "data": [
        { "title": "Terminal.app", "value": "Terminal" },
        { "title": "iTerm2", "value": "iTerm" }
      ]
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.94.0",
    "@raycast/utils": "^1.19.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "test": "vitest run",
    "publish": "npx @raycast/api@latest publish"
  }
}
