{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "agentcfg",
  "title": "Agentcfg",
  "description": "Manage agentcfg from Raycast: browse items, sync configs, and check target status.",
  "icon": "extension-icon.png",
  "author": "jorgenosberg",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "manage-items",
      "title": "Manage Items",
      "description": "Browse items, see per-target install state, and toggle, install, or sync directly.",
      "mode": "view"
    },
    {
      "name": "sync-configs",
      "title": "Sync Configs",
      "description": "Run agentcfg sync across all configured agent targets.",
      "mode": "no-view"
    }
  ],
  "preferences": [
    {
      "name": "binaryPath",
      "title": "agentcfg Binary",
      "description": "Path to the agentcfg binary. Leave empty to auto-detect from common install locations.",
      "type": "textfield",
      "required": false,
      "placeholder": "/opt/homebrew/bin/agentcfg"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.23",
    "@raycast/utils": "^2.2.7"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}